#!/usr/bin/env python3
"""
Prebuilt patch apply for the anthropic.claude-code VS Code extension 2.1.195.

All patches applied as literal string replacements verified
byte-stable against the 2.1.195 bundle. Synthesized by
util/build-prebuilt.py from the diff between the patched live extension
and its pre-patch backups.

Usage:
  python3 apply.py [/path/to/extension/dir] [--force]

Default: auto-discovers an installed 2.1.195 extension under
~/.<ide>/extensions/ for any IDE that pulls from Open VSX (VS Code,
Antigravity, Cursor, VSCodium, etc.).

Idempotent: re-running on already-patched files is a no-op (detects the
/*pfg-v1.11*/ signature in extension.js). With --force, restores from .bak files
and re-applies.
"""
import glob
import os
import re
import shutil
import subprocess
import sys

VERSION = "2.1.195"


def find_default_ext_dir():
    # Strongest signal: CLAUDE_CODE_EXECPATH (set by the IDE-hosted Claude
    # Code CLI) points at the extension install of the running session.
    # Caveat: standalone CLI layout (~/.local/share/claude/...) also sets
    # this var but isn't an extension install. Match the name pattern
    # explicitly so it falls through to the glob fallback in that case.
    execpath = os.environ.get("CLAUDE_CODE_EXECPATH", "")
    if execpath:
        target = f"anthropic.claude-code-{VERSION}-linux-x64"
        parts = execpath.split("/")
        for i, p in enumerate(parts):
            if p == target:
                candidate = "/" + "/".join(parts[1:i+1])
                if os.path.isdir(candidate):
                    return candidate
                break

    pattern = os.path.expanduser(
        f"~/.*/extensions/anthropic.claude-code-{VERSION}-linux-x64"
    )
    matches = glob.glob(pattern)
    if not matches:
        return None
    if len(matches) > 1:
        sys.exit(
            f"Multiple installs of {VERSION} detected. Pass the path "
            f"explicitly, or invoke from inside the IDE you want to patch "
            f"(CLAUDE_CODE_EXECPATH disambiguates):\n  "
            + "\n  ".join(matches)
        )
    return matches[0]


SIGNATURE = "/*pfg-v1.11*/"
PATCHSET_VERSION = re.match(r'/\*pfg-v(\d+(?:\.\d+)?)\*/', SIGNATURE).group(1)

# Each entry: (file_relpath, [splice_dict, ...]); each splice_dict has keys
# "old", "new", "expected_count" (mirrors util/test_splices.py:apply_splices).
SPLICES = [('extension.js', [{'old': '}async function r1e(e,t){try{if(t>HAe&&!ui(process.env.CLAUDE_CODE_DISABLE_PRECOMPACT_SKIP))return(await t1e(e,t)).postBoundaryBuf', 'new': '}async function r1e(e,t){try{if(t>HAe&&!(!0||ui(process.env.CLAUDE_CODE_DISABLE_PRECOMPACT_SKIP)))return(await t1e(e,t)).postBoundaryBuf', 'expected_count': 1}, {'old': 'catch(n){var r=n,i=1}finally{zr(t,r,i)}}async function i1e(e){let t=new Map;for(let f of e)t.set(f.uuid,f);let r=0;for(let f of t.values()){if(f.type!=="system"||f.subtype!=="compact_boundary")continue;let m=f.compactMetadata?.preservedMessages,v=f.compactMetadata?.preservedSegment;if(m){if(m.uuids.length===0||m.uuids.some((b)=>!t.has(b)))continue;let y=m.anchorUuid;for(let b of m.uuids){let w=t.get(b);t.set(b,{...w,parentUuid:y}),y=b}let h=m.uuids[0],_=m.uuids.at(-1);for(let[b,w]of t){if(++r%NJ===0)await new Promise((E)=>setImmediate(E));if(w.parentUuid===m.anchorUuid&&b!==h)t.set(b,{...w,parentUuid:_})}}else if(v){let y=t.get(v.headUuid);if(y)t.set(v.headUuid,{...y,parentUuid:v.anchorUuid});for(let[h,_]of t){if(++r%NJ===0)await new Promise((b)=>setImmediate(b));if(_.parentUuid===v.anchorUuid&&h!==v.headUuid)t.set(h,{..._,parentUuid:v.tailUuid})}}}let i=new Map;for(let f=0;f<e.length;f++)i.set(e[f].uuid,f);let n=new Set;for(let f of t.values())if(f.parentUuid)n.add(f.parentUuid);let s=[...t.values()].filter((f)=>!n.has(f.uuid)),o=[];for(let f of s){let m=f,v=new Set;while(m){if(v.has(m.uuid))break;if(v.add(m.uuid),m.type==="user"||m.type==="assistant"){o.push(m);break}m=m.parentUuid?t.get(m.parentUuid):void 0}}if(o.length===0)return[];let a=o.filter((f)=>!f.isSidechain&&!f.teamName&&!f.isMeta),c=(f)=>f.reduce((m,v)=>(i.get(v.uuid)??-1)>(i.get(m.uuid)??-1)?v:m),l=a.length>0?c(a):c(o),u=[],d=new Set,p=t.get(l.uuid);while(p){if(d.has(p.uuid))break;d.add(p.uuid),u.push(p),p=p.parentUuid?t.get(p.parentUuid):void 0}return u.reverse(),await new Promise((f)=>setImmediate(f)),o1e(t,u,d)}function IA(e){if(e.type!=="assistant")r', 'new': 'catch(n){var r=n,i=1}finally{zr(t,r,i)}}/*pfg-v1.11*/\n// ==== pfg-core (GENERATED from src/pfg-core.js by util/pfg-codegen.py) ====\n// Do NOT edit here. Edit src/pfg-core.js and re-run the codegen. This is the ONE\n// injected definition of the compaction/lineage primitives (the extension subset:\n// only the members this target uses, plus their transitive deps).\n/*\n * pfg-core: single source of truth for the compaction/lineage primitives that\n * Patches D, J, and K depend on.\n *\n * WHY THIS FILE EXISTS: the patches were authored as hand-written splices into a\n * minified bundle, so "is this a compaction boundary", "which edge does the walk\n * follow", and "is this a conversation root" each got re-decided inline at many\n * sites, and they drifted apart: the live loader\'s reachability check follows\n * only parentUuid while the display walk and the loss bookend follow the logical\n * parent too, so they disagree about where a lineage ends. This module defines\n * each of those concepts ONCE.\n *\n * The codegen (util/pfg-codegen.py) inlines this into the bundle as one shared\n * block that both the loader (d1e) and the chain-builder (i1e) reference by\n * name. Until those call sites are redirected to it and the inline copies are\n * deleted, this module is the authoritative definition the copies must match,\n * not a second opinion competing with them.\n */\nconst $pfg = (function () {\n\n// The stable text a resumed/continued session opens with. A turn carrying this\n// is a continuation summary, not the first prompt, even when its structure\n// (null parent, no lpu, not flagged isCompactSummary) looks like one.\nconst CONTINUATION_PREAMBLE =\n  "This session is being continued from a previous conversation";\n\n// The uuid prefix every Patch-K marker ghost carries (resplice banner, seam,\n// bookend, bridge, ...). One constant so the many inline "pfgk-" string tests\n// stop drifting between .slice(0,5), .indexOf(...)===0, and .startsWith.\nconst GHOST_PREFIX = "pfgk-";\n\n// The PFGK1 payload envelope. i1e/d1e stamp a marker as\n// message.content = PFGK1_PREFIX + JSON.stringify(payload); the webview detects a\n// ghost by this prefix and reads the payload (kind, bands, rows, ...) as data,\n// never re-deriving from the uuid. One definition, both the exthost emit and the\n// webview consume link to it.\nconst PFGK1_PREFIX = "PFGK1:";\n\n// A compaction boundary record.\nconst isBoundary = (rec) =>\n  !!rec && rec.type === "system" && rec.subtype === "compact_boundary";\n\n// Is this uuid one of our planted marker ghosts? String form, for when only the\n// uuid is in hand (e.g. a logicalParentUuid that points at a seam ghost).\nconst isGhostUuid = (uuid) =>\n  typeof uuid === "string" && uuid.startsWith(GHOST_PREFIX);\n\n// A Patch-K marker ghost record.\nconst isGhost = (rec) => !!rec && isGhostUuid(rec.uuid);\n\n// The KIND of a marker ghost, parsed from its uuid ("pfgk-<kind>-<slice>"):\n// "seam" | "seamClean" | "bridge" | "resplice" | "broken" | "bookend", else null.\n// The one place that reads a ghost\'s kind, so a caller telling an UNVERIFIED seam\n// reattachment (K2: the phantom logical parent was never found, so the chain is\n// reattached to the in-file predecessor by position) from a VERIFIED bridge (K3:\n// the logical parent was resolved in-file or cross-file) does not restate the\n// prefix grammar. Construction is GHOST_PREFIX + kind + "-" + slice, so the first\n// "-" after the prefix delimits the kind. Note "seam" is a strict prefix of\n// "seamClean"; splitting on that first "-" keeps them distinct.\nconst ghostKind = (rec) => {\n  const uuid = rec && rec.uuid;\n  if (typeof uuid !== "string" || !uuid.startsWith(GHOST_PREFIX)) return null;\n  const rest = uuid.slice(GHOST_PREFIX.length);\n  const dash = rest.indexOf("-");\n  return dash === -1 ? rest : rest.slice(0, dash);\n};\n\n// THE lineage edge: the real parent, else the logical parent. A compaction writes\n// the logicalParentUuid on its boundary record, pointing across the boundary at the\n// pre-compaction tail; omitting it here is what makes a walk dead-end at every\n// compaction boundary, so following it IS the point of Patches D/J/K. (Measured\n// across the real corpus: a logicalParentUuid appears ONLY on compact_boundary\n// records (lpu is boundary-only). A fork/resume ALSO stamps forkedFrom {sessionId,\n// messageUuid} on every copied record (all types), naming its off-disk source;\n// forkedFrom is NOT disjoint from the lpu (a rare boundary-headed fork carries both),\n// and d1e\'s F step resolves it to a real parent edge at LOAD, so edge() itself needs\n// no forkedFrom awareness. An earlier draft claimed forks write the lpu across files;\n// the data does not bear that out.)\n//\n// `??`, not `||`: parentUuid / logicalParentUuid are each a uuid string or\n// null/undefined. Nullish-coalescing falls through to the logical parent only for a\n// genuinely absent parentUuid; a present-but-falsy value (e.g. "") does not silently\n// reroute the lineage. There is no valid falsy uuid, so on real data this is\n// identical to ||; it is only more defensive on malformed data, a choice made on\n// purpose rather than inherited from the minified original.\nconst edge = (rec) =>\n  (rec && (rec.parentUuid ?? rec.logicalParentUuid)) ?? null;\n\n// A file-local conversation turn: user or assistant, not meta or sidechain.\n// `teamName` identifies the owner of a teammate transcript; it does NOT make that\n// teammate\'s own turns foreign to the file. Anthropic\'s leader-view renderer uses\n// `!teamName` only as a preference when a parent transcript mixes candidate tips.\n// Promoting that preference into this shared predicate made team-only transcripts\n// rootless and emptied their resplice. This is the BASE predicate that counting,\n// banding, the resplice spine, and marker logic build on; each layers its own\n// further filters (the resplice spine also excludes tool-result carriers and pfgk-\n// ghosts). isMain is not itself the spine.\nconst isMain = (rec) =>\n  !!rec && (rec.type === "user" || rec.type === "assistant") &&\n  !rec.isMeta && !rec.isSidechain;\n\n// Plain-text content of a turn, for the one decision structure cannot make.\nconst contentText = (rec) => {\n  const content = rec && rec.message && rec.message.content;\n  if (typeof content === "string") return content;\n  if (Array.isArray(content))\n    for (const block of content)\n      if (block && block.type === "text" && typeof block.text === "string")\n        return block.text;\n  return "";\n};\n\n// A STRUCTURAL conversation origin: a main-line turn with no parent and no\n// logical parent, that is not itself a compaction artifact. Necessary but NOT\n// sufficient to BE the root: BOTH a continuation preamble AND a forkedFrom copy\n// satisfy every clause here yet are not the origin (their real first prompt is off\n// disk). isOrigin deliberately does NOT exclude them, so the loader\'s fork-discovery\n// still sees a fork copy as a structural origin to resolve; classifyRoot owns the\n// preamble and fork verdicts. See isContinuationPreamble and classifyRoot.\nconst isOrigin = (rec) =>\n  isMain(rec) && !edge(rec) && !rec.isCompactSummary && !isGhost(rec);\n\n// A structural origin whose CONTENT is a continuation summary: the deepest\n// root recoverable from disk, but the first prompt predates it and is off\n// disk, so this is a dead end, not the origin. Distinguished so we recover TO\n// it without CLAIMING it is the origin.\nconst isContinuationPreamble = (rec) =>\n  isOrigin(rec) && contentText(rec).startsWith(CONTINUATION_PREAMBLE);\n\n// The verdict on a single terminus, split along the two axes docs/invariant.md\n// requires so they can never be conflated:\n//   reachedRoot: the boolean VERDICT. true only for an origin.\n//   reason: the DATA behind it; never softens the verdict.\n//     "origin":   the first turn; the transcript may render as complete.\n//     "preamble": deepest on-disk root is a continuation summary, a dead end;\n//                 the origin is off disk. A FAILURE (reachedRoot=false),\n//                 routed to the loud marker, never a soft note.\n//     "fork":     structural origin whose ancestry is an off-disk SOURCE session\n//                 (forkedFrom), not yet resolved complete. A FAILURE\n//                 (reachedRoot=false), routed to the loud marker. Parallel to\n//                 preamble, on the cross-session axis.\n//     "none":     terminus is a boundary / summary / non-origin dead-end.\n// Splitting the axes is the whole point: a renderer keying on the boolean cannot\n// paint a preamble as success, because for a preamble the boolean is false.\nconst classifyRoot = (rec) => {\n  if (!isOrigin(rec)) return { reachedRoot: false, reason: "none" };\n  if (isContinuationPreamble(rec))\n    return { reachedRoot: false, reason: "preamble" };\n  // A fork copy is a structural origin whose real ancestry is an off-disk source\n  // session named by forkedFrom. It is a dead end until the loader confirms the\n  // copy itself is that source\'s origin (__pfgkForkComplete). Same masquerade as\n  // the preamble, on the cross-session axis; both fields sit on this one record,\n  // so the verdict stays local. Keeping this OUT of isOrigin is deliberate: the\n  // loader\'s fork-discovery loop needs isOrigin(rec) && rec.forkedFrom to still see\n  // a fork copy as a structural origin, or it would never find forks to resolve.\n  if (rec.forkedFrom && rec.forkedFrom.sessionId && !rec.__pfgkForkComplete)\n    return { reachedRoot: false, reason: "fork" };\n  return { reachedRoot: true, reason: "origin" };\n};\n\n// Build a uuid -> record index (last-wins on duplicate uuids), the shape both\n// walkToRoot and the loader\'s cross-file backfill consume. One builder so the\n// `_mapOf`-style rebuild stops being re-authored inline.\nconst byUuid = (records) => {\n  const index = new Map();\n  for (const rec of records) if (rec && rec.uuid) index.set(rec.uuid, rec);\n  return index;\n};\n\n// Walk from a uuid along `edge`, cycle-guarded, reporting where and how it ends.\n// ONE predicate for every caller (the loader\'s backfill decision, the loader\'s\n// edge pickers, and the chain-builder\'s main walk), so they can never disagree.\n// This is the MECHANISM: it returns the raw visited path; the display/trust POLICIES\n// (crossedLpu, rootTrusted, crossedUnprovenSeam) fold that path, rather than the walk\n// baking an any-lpu / any-seam boolean into itself. Returns:\n//   { terminus, reachedRoot, reason, path, steps, ref? }\n//   reason: "origin" | "preamble" | "fork" | "none" | "cycle" | "dangling"\n//     (origin / preamble / fork come from classifyRoot at the terminus; cycle and\n//     dangling are walk outcomes.)\n//   path: the records visited, in walk order (leaf -> terminus). EVERY crossing the\n//     policies care about is IN this path: a logical-parent hop (crossedLpu) and any\n//     K2 seam ghost (crossedUnprovenSeam / rootTrusted) are derived from it, so the\n//     walk never pre-computes them. The chain-builder also consumes path as its\n//     walked set instead of re-walking the lineage inline.\n//   ref (on "dangling" only): the off-file uuid the chain points at, a backfill\n//     candidate the loader looks for in sibling files.\n//\n// LOADER kind->action contract: cross-file backfill continues UNLESS\n// reachedRoot. preamble / fork / none / cycle / dangling all keep the search\n// going; a preamble must NOT stop it, or we refuse to recover a root\n// sitting in a sibling file. Backfill ends only when genuinely exhausted (no\n// sibling adds new records); if the terminus is then still not an origin, that\n// is the invariant\'s clause-2 loud hard failure, never a quiet green stop.\nconst walkToRoot = (startUuid, index) => {\n  let cur = index.get(startUuid), seen = new Set(), steps = 0;\n  const path = [];\n  const buildResult = (reason, terminus, extra) => ({\n    terminus,\n    reachedRoot: reason === "origin",\n    reason,\n    path,\n    steps,\n    ...extra,\n  });\n  while (cur) {\n    // The terminus on a cycle is the last DISTINCT node reached (path end), not the\n    // already-visited node the edge loops back to: callers band post-boundary\n    // reachability off this terminus, so it must be the node the walk ended on,\n    // matching a plain leaf -> dead-end walk.\n    if (seen.has(cur.uuid)) return buildResult("cycle", path[path.length - 1]);\n    seen.add(cur.uuid);\n    path.push(cur);\n    steps++;\n    if (isOrigin(cur)) return buildResult(classifyRoot(cur).reason, cur);\n    const ref = edge(cur);\n    if (!ref) return buildResult("none", cur);\n    const next = index.get(ref);\n    if (!next) return buildResult("dangling", cur, { ref });\n    cur = next;\n  }\n  return buildResult("none", null);\n};\n\n// Display signal (formerly walkToRoot\'s crossedLpu boolean): did the walk traverse at\n// least one logical-parent edge, i.e. was the lineage recovered ACROSS a compaction/fork\n// boundary rather than by the plain parent chain? Re-derived from the returned path, so\n// walkToRoot reports the raw crossings (mechanism) and this folds them (policy) instead\n// of baking the fold into the walk. A path node hopped via its logical parent iff its\n// parentUuid is null AND the walk continued past it (it is not the terminus): the terminus\n// origin also has a null parent but is last, so it is correctly excluded. An empty-string\n// parentUuid dead-ends at "none" and is not == null, so it never counts as a cross.\nconst crossedLpu = (path) =>\n  Array.isArray(path) &&\n  path.some((rec, i) => rec && rec.parentUuid == null && i < path.length - 1);\n\n// A K2 seam ghost whose same-conversation reattachment is NOT proven: either no evidence\n// was stamped at plant time, or the evidence says the reattach target is off this\n// conversation\'s lineage (a forkedFrom copy). The trust-BLOCKING case; a seam with\n// proven:true is a verified bridge and does not block. Internal to the two folds below.\nconst seamUnproven = (rec) =>\n  ghostKind(rec) === "seam" && !(rec.__pfgkSeam && rec.__pfgkSeam.proven === true);\n\n// The TRUST verdict (policy over walkToRoot\'s path mechanism, formerly the crossedSeam\n// boolean, now proof-promoting): the walk reached a genuine origin AND no unproven seam\n// sits on the path. seamClean/bridge ghosts are already verified reattachments (not\n// "seam"), so only the unverified positional reattach must clear the bar. With no evidence\n// stamped, every seam is unproven, so this collapses to the pre-promotion rule reachedRoot\n// && no-seam. The gradation lives in the boolean `proven` (binary AT proof), never a\n// continuous score reaching the render.\nconst rootTrusted = (walk) =>\n  !!walk &&\n  walk.reachedRoot === true &&\n  Array.isArray(walk.path) &&\n  !walk.path.some(seamUnproven);\n\n// Did the walk cross an unproven seam? The display counterpart to rootTrusted: it names the\n// specific failure (root reattached by unverified in-file position) for the marker\'s honest\n// detail, distinct from a preamble / fork / plain non-origin dead end.\nconst crossedUnprovenSeam = (path) =>\n  Array.isArray(path) && path.some(seamUnproven);\nreturn { GHOST_PREFIX, PFGK1_PREFIX, byUuid, classifyRoot, crossedLpu, crossedUnprovenSeam, edge, ghostKind, isBoundary, isGhost, isGhostUuid, isMain, isOrigin, rootTrusted, walkToRoot };\n})();\n// ==== end pfg-core ====\nconst _pv_GY = GY, _pv_MY = require("fs/promises"), _pv_Nk = Nk, _pv_n1e = n1e, _pv_o1e = o1e, _pv_qAe = qAe, _pv_r1e = r1e, _pv_s1e = s1e, _pv_zn = require("path");\n/*pfg-v1.11*//*\n * i1e: the chain-builder / render walker (Patches D + K). Wholesale readable\n * replacement of the vendor i1e; the apply step injects it after the $pfg block.\n *\n * It reparents the compaction working set, walks the lineage from the newest\n * turn back to the root, and decides how to render:\n *   - healthy chain -> normal render, plus a bookend at the root (green if the\n *     origin was reached, the loud failure marker if not);\n *   - corrupt chain (a cycle, or history stranded off the canonical walk) ->\n *     resplice every saved message in write order under a CHAIN CORRUPT banner,\n *     with per-message context banding, plus the same root verdict on top.\n *\n * THE FIX (docs/invariant.md): both root checks (the corrupt-path oldest-turn\n * check and the healthy-path terminus check) go through $pfg.classifyRoot, which\n * reads content, so a continuation preamble is a hard failure that fires the loud\n * marker, never a silent green "origin reconstructed" bookend.\n *\n * SSOT: every boundary/edge/origin/ghost decision I own or that is\n * behaviour-equivalent goes through $pfg. The two vendor selection heuristics\n * (the leaf-find collection `_leafCandidates` and the max-index pick `_pickNewest`) are preserved as-is.\n *\n * Vendor deps are referenced through the _pv_ namespace (see d1e.js): the driver\n * derives the set by grepping /_pv_\\w+/ and binds each via a discovered alias\n * prologue. Each dep\'s identity lives once, in the anchor registry\n * (pfg/anchors.py), not restated here. NJ (the setImmediate yield-batch size) is\n * inlined below as the literal 8192 to shrink the surface by one name; a tuning\n * constant, not behaviour.\n */\nasync function i1e(_records, _telemetry) {\n  let _byUuid = new Map();\n  for (let _rec of _records) _byUuid.set(_rec.uuid, _rec);\n  let _yieldCounter = 0;\n\n  // ---- vendor: compaction reparent (osc relink). Logic preserved; the boundary\n  // ---- test goes through $pfg. My addition: skip a boundary whose logical\n  // ---- parent is already present (it is not a dangling reparent target).\n  for (let _rec of _byUuid.values()) {\n    if (!$pfg.isBoundary(_rec)) continue;\n    if (_rec.logicalParentUuid && _byUuid.has(_rec.logicalParentUuid)) continue;\n    let _preserved = _rec.compactMetadata?.preservedMessages,\n      _segment = _rec.compactMetadata?.preservedSegment;\n    if (_preserved) {\n      if (_preserved.uuids.length === 0 || _preserved.uuids.some((_uuid) => !_byUuid.has(_uuid))) continue;\n      let _parentCursor = _preserved.anchorUuid;\n      for (let _uuid of _preserved.uuids) {\n        let _preservedRec = _byUuid.get(_uuid);\n        _byUuid.set(_uuid, { ..._preservedRec, parentUuid: _parentCursor });\n        _parentCursor = _uuid;\n      }\n      let _headUuid = _preserved.uuids[0],\n        _tailUuid = _preserved.uuids.at(-1);\n      for (let [_uuid, _otherRec] of _byUuid) {\n        if (++_yieldCounter % 8192 === 0) await new Promise((_resolve) => setImmediate(_resolve));\n        if (_otherRec.parentUuid === _preserved.anchorUuid && _uuid !== _headUuid) _byUuid.set(_uuid, { ..._otherRec, parentUuid: _tailUuid });\n      }\n    } else if (_segment) {\n      let _headRec = _byUuid.get(_segment.headUuid);\n      // Guard like the preservedMessages path: if the segment\'s head or tail is not\n      // present, skip. Otherwise the sweep below reparents records to an absent\n      // tailUuid, creating a dangling parentUuid that dead-ends the canonical walk.\n      if (!_headRec || !_byUuid.has(_segment.tailUuid)) continue;\n      _byUuid.set(_segment.headUuid, { ..._headRec, parentUuid: _segment.anchorUuid });\n      for (let [_uuid, _otherRec] of _byUuid) {\n        if (++_yieldCounter % 8192 === 0) await new Promise((_resolve) => setImmediate(_resolve));\n        if (_otherRec.parentUuid === _segment.anchorUuid && _uuid !== _segment.headUuid) _byUuid.set(_uuid, { ..._otherRec, parentUuid: _segment.tailUuid });\n      }\n    }\n  }\n\n  // vendor: last-occurrence index (the resplice below uses its own first-occurrence\n  // index for chronological order; this one is not chronological).\n  let _lastIdx = new Map();\n  for (let _i = 0; _i < _records.length; _i++) _lastIdx.set(_records[_i].uuid, _i);\n\n  // vendor: roots are records nobody parents; find the leaf turn under each. The\n  // per-root walk follows $pfg.edge (my change: cross the logical parent too, so a\n  // compaction does not hide the leaf). The user/assistant collection stays as the\n  // vendor\'s loose leaf-find heuristic.\n  let _parentedUuids = new Set();\n  for (let _rec of _byUuid.values()) if (_rec.parentUuid) _parentedUuids.add(_rec.parentUuid);\n  let _roots = [..._byUuid.values()].filter((_rec) => !_parentedUuids.has(_rec.uuid)),\n    _leafCandidates = [];\n  for (let _root of _roots) {\n    let _rec = _root,\n      _visited = new Set();\n    while (_rec) {\n      if (_visited.has(_rec.uuid)) break;\n      _visited.add(_rec.uuid);\n      if (_rec.type === "user" || _rec.type === "assistant") { _leafCandidates.push(_rec); break; }\n      let _ref = $pfg.edge(_rec);\n      _rec = _ref ? _byUuid.get(_ref) : void 0;\n    }\n  }\n  if (_leafCandidates.length === 0) return [];\n\n  // Prefer a real file-local leaf; fall back to any. `_mainLeaves`\'s filter is exactly\n  // $pfg.isMain (_leafCandidates is already user/assistant, so the remaining meta/sidechain\n  // exclusion IS isMain; teamName is local ownership in teammate files). `_pickNewest` is\n  // the vendor max-by-index pick, preserved.\n  let _mainLeaves = _leafCandidates.filter((_rec) => $pfg.isMain(_rec)),\n    _pickNewest = (_candidates) => _candidates.reduce((_newest, _cand) => ((_lastIdx.get(_cand.uuid) ?? -1) > (_lastIdx.get(_newest.uuid) ?? -1) ? _cand : _newest)),\n    _leaf = _mainLeaves.length > 0 ? _pickNewest(_mainLeaves) : _pickNewest(_leafCandidates);\n\n  // ---- guard #5: walk leaf -> root through the shared $pfg.walkToRoot. It carries\n  // ---- the visited path, the crossed-lpu flag (bookend says "reconstructed"), the\n  // ---- crossed-seam flag (K2 positional reattach -> root unverified), and a cycle\n  // ---- reason, so this chain-builder no longer re-derives the walk inline. The same\n  // ---- cycle guard kills the 4044ab66 lpu-into-preserved-tail and 9da63ece\n  // ---- forward-edge cycles; on a cycle the terminus is the last distinct node\n  // ---- reached, matching the old inline u[last].\n  let _walk = $pfg.walkToRoot(_leaf.uuid, _byUuid),\n    _renderOrder = _walk.path,\n    _renderOrderUuids = new Set(_renderOrder.map((_rec) => _rec.uuid)),\n    _term = _walk.terminus,\n    // Policy folds over the walk\'s path (mechanism): recovered = crossed a compaction/fork\n    // edge (bookend says "reconstructed"); crossedUnprovenSeam = a K2 seam on the path whose\n    // same-conversation reattachment is not proven (root unverified). See pfg-core.\n    _recovered = $pfg.crossedLpu(_walk.path),\n    _crossedUnprovenSeam = $pfg.crossedUnprovenSeam(_walk.path),\n    _cycleHit = _walk.reason === "cycle";\n\n  // ---- outcome-gated corruption detection. _pfStranded: some unwalked main-line\n  // ---- node has NO route to the canonical visited set _renderOrderUuids (a disjoint / dead-ended\n  // ---- block). A healthy rewind/edit fork rejoins _renderOrderUuids at its fork point.\n  let _walkMain = 0;\n  for (let _rec of _renderOrder) if ($pfg.isMain(_rec)) _walkMain++;\n  let _fileMain = 0;\n  for (let _rec of _byUuid.values()) if ($pfg.isMain(_rec)) _fileMain++; // _byUuid is last-wins, so this is the UNIQUE count\n  async function _pfStranded(_byUuid, _renderOrderUuids) {\n    let _ops = 0,\n      _joins = new Set(_renderOrderUuids);\n    for (let _start of _byUuid.values()) {\n      if (!$pfg.isMain(_start) || _renderOrderUuids.has(_start.uuid) || _joins.has(_start.uuid)) continue;\n      let _cursor = _start,\n        _seen = new Set(),\n        _path = [],\n        _reachedCanon = false;\n      while (_cursor) {\n        if (++_ops % 8192 === 0) await new Promise((_resolve) => setImmediate(_resolve));\n        if (_joins.has(_cursor.uuid)) { _reachedCanon = true; break; } // reached the canonical chain (or a known-good node)\n        if (_seen.has(_cursor.uuid)) break; // local cycle without joining -> stranded\n        _seen.add(_cursor.uuid);\n        _path.push(_cursor.uuid);\n        let _ref = $pfg.edge(_cursor);\n        if (!_ref) break; // reached a SEPARATE clean root -> disjoint tree\n        _cursor = _byUuid.get(_ref);\n      }\n      if (_reachedCanon) { for (let _uuid of _path) _joins.add(_uuid); } // memoize the whole good path\n      else return true; // a main-line turn with no path to canonical -> corrupt\n    }\n    return false;\n  }\n  let _corrupt = _cycleHit;\n  if (!_corrupt && _walkMain < _fileMain) _corrupt = await _pfStranded(_byUuid, _renderOrderUuids);\n\n  // Capture canonical reachability + terminus BEFORE the resplice reassigns _renderOrderUuids / nulls _term.\n  let _term0 = _term,\n    _canon = _renderOrderUuids,\n    _pfBan = null;\n\n  // THE root verdict, computed once and shared by both render paths. It is always\n  // a statement about the LIVE leaf\'s lineage: did the walk from the newest turn\n  // (_leaf) reach an origin by a VERIFIED path? Keyed on _term0 (the live leaf\'s\n  // terminus), never on whichever fragment sorts oldest in the resplice: keying on\n  // _renderOrder[0] there let a disjoint side-tree\'s origin suppress the marker while the live\n  // lineage dead-ended. A crossed K2 seam is a positional reattachment whose root\n  // is unproven, so it never counts as reaching the root. classifyRoot reads\n  // content, so a continuation preamble is a failure here, not a success.\n  let _rootCls = _term0 ? $pfg.classifyRoot(_term0) : { reachedRoot: false, reason: "none" };\n  // An unresolved forkedFrom origin (source off-disk / copy absent from it) is a\n  // dead end, not an origin: the first prompt lives in a source session we cannot\n  // read, so completeness is undecidable. classifyRoot now OWNS this verdict\n  // (reason "fork", reachedRoot false), parallel to the preamble dead end, so there\n  // is no separate _forkUnresolved bolt-on here anymore. A soft "forked from..."\n  // note would be a soft note for a hard failure (a forbidden middle); it is the\n  // loud marker instead. A RESOLVED fork never lands on "fork": d1e either re-rooted\n  // a mid-conversation branch (the terminus is the source\'s own origin) or flagged a\n  // branch-at-source-start __pfgkForkComplete (classifyRoot -> origin -> green).\n  // Divergent-lpu loud detect (never degrade silently). $pfg.byUuid is last-wins, so a\n  // corrupt uuid-collision carrying TWO distinct logicalParentUuids would silently keep one\n  // and could walk to a foreign origin: a false green. Structurally precluded in well-formed\n  // data (a boundary\'s lpu is written once at compaction and copied verbatim on a fork, so it\n  // cannot diverge; measured 0 across 146k uuids), but this complex exists FOR corrupted\n  // lineage, so detect it over the assembled set and FAIL the trust verdict loud rather than\n  // silently mis-pick. Detected here, not in d1e: i1e sees the identical assembled set in\n  // _records and owns the verdict, so no cross-component flag is needed.\n  let _divergentLpu = false;\n  {\n    let _lpuOf = new Map();\n    for (let _rec of _records) {\n      if (!_rec || !_rec.uuid || _rec.logicalParentUuid == null) continue;\n      let _prev = _lpuOf.get(_rec.uuid);\n      if (_prev === undefined) _lpuOf.set(_rec.uuid, _rec.logicalParentUuid);\n      else if (_prev !== _rec.logicalParentUuid) { _divergentLpu = true; break; }\n    }\n  }\n  let _reachedRoot = $pfg.rootTrusted(_walk) && !_divergentLpu;\n  // The honest reason the live conversation\'s root is not present, shared by both\n  // markers. `_where` names the terminus in that path\'s language.\n  let _failDetail = (_where) =>\n    _divergentLpu\n      ? "A saved message appears more than once with DIFFERENT parent-compaction links (a corrupt uuid collision), so this conversation’s lineage cannot be trusted to a single root."\n      : _rootCls.reason === "fork"\n      ? "This conversation was branched (/fork) from a source session that is not saved in this folder, so the turns before the branch point, and the conversation’s first prompt, cannot be read from disk."\n      : _crossedUnprovenSeam\n      ? "This conversation’s compaction referenced a parent that was never saved to disk, so Patch K reattached the chain by position to the nearest preceding record. The origin shown is that record’s own; it cannot be confirmed as this conversation’s first prompt."\n      : _rootCls.reason === "preamble"\n      ? "The " + _where + " is a continuation summary (“This session is being continued…”), so the conversation’s first prompt predates the saved files."\n      : "The walk from the latest turn reached " +\n        (_term0 ? String(_term0.uuid).slice(0, 8) + " (" + _term0.type + ")" : "no record") +\n        ", which is not a conversation origin, so the beginning of the conversation is not present.";\n\n  if (_corrupt) {\n    // ----- first-occurrence index = chronological order key (NOT vendor _lastIdx, which is\n    //       last-occurrence). Computed up front: the resume-boundary pick keys on it. -----\n    let _firstIdx = new Map();\n    for (let _k = 0; _k < _records.length; _k++) if (!_firstIdx.has(_records[_k].uuid)) _firstIdx.set(_records[_k].uuid, _k);\n    // ----- locate the boundary the MODEL resumes from, NOT the physically-last one. The\n    //       physically-last compact_boundary can be a re-appended OLDER duplicate (measured\n    //       on 9da: 586fd2dc re-appended after the real compaction c9a6635f), whose\n    //       preservedMessages describe a different, older context; keying the in-context\n    //       band on it makes "in resume context: N" confidently wrong (~118 vs the real 8).\n    //       The boundary the model resumed from is the FIRST compaction reached walking UP\n    //       from the live leaf (the leaf is its descendant), so take it off the live-leaf\n    //       walk path; fall back to physical-last only if the walk reached no boundary. -----\n    let _lastBoundary = null,\n      _lastBoundaryIdx = -1;\n    for (let _rec of _walk.path)\n      if ($pfg.isBoundary(_rec)) { _lastBoundary = _rec; break; }\n    if (!_lastBoundary)\n      for (let _k = 0; _k < _records.length; _k++)\n        if ($pfg.isBoundary(_records[_k])) _lastBoundary = _records[_k];\n    if (_lastBoundary) _lastBoundaryIdx = _firstIdx.get(_lastBoundary.uuid) ?? -1;\n    // _postReliable: the canonical walk reached/stopped at the boundary (or an\n    // adjacent planted ghost), so _canon is COMPLETE for the post-boundary region\n    // and we may band post-boundary reachability with confidence. If the walk died\n    // mid-post-compaction, we cannot confirm membership and must hedge.\n    let _postReliable =\n      !!_term0 && ($pfg.isBoundary(_term0) || !!_term0.isCompactSummary || $pfg.isGhost(_term0));\n    // ----- authoritative in-context membership: boundary union preservedMessages\n    //       (PRIMARY), preservedSegment [head..tail] (SECONDARY), boundary-only\n    //       best-effort (TERTIARY) -----\n    let _live = new Set(),\n      _bandMode = "bestEffort";\n    if (_lastBoundary) {\n      _live.add(_lastBoundary.uuid);\n      let _preserved = _lastBoundary.compactMetadata?.preservedMessages,\n        _segment = _lastBoundary.compactMetadata?.preservedSegment;\n      if (_preserved && Array.isArray(_preserved.uuids)) {\n        for (let _uuid of _preserved.uuids) _live.add(_uuid);\n        _bandMode = "preserved";\n      } else if (_segment && _segment.headUuid) {\n        let _cursor = _byUuid.get(_segment.tailUuid) || _byUuid.get(_segment.headUuid),\n          _visited = new Set();\n        while (_cursor && !_visited.has(_cursor.uuid)) {\n          _visited.add(_cursor.uuid);\n          _live.add(_cursor.uuid);\n          if (_cursor.uuid === _segment.headUuid) break;\n          _cursor = _byUuid.get($pfg.edge(_cursor)); // follow the real edge (lpu too), not just parentUuid\n        }\n        _bandMode = "segment";\n      }\n    }\n    // ----- per-record band. Post-boundary content the canonical walk did not reach\n    //       is NEVER confident LIVE: "severed" (proven orphan) or "uncertain". -----\n    let _liveCount = 0,\n      _summarizedCount = 0,\n      _severedCount = 0,\n      _uncertainCount = 0,\n      _liveUuids = [],\n      _severedUuids = [],\n      _uncertainUuids = []; // summarized is the implicit default; list only live/severed/uncertain\n    for (let _rec of _byUuid.values()) {\n      if (!$pfg.isMain(_rec) || $pfg.isGhost(_rec)) continue; // ghosts pass isMain; keep them out of the counts\n      let _firstAppearance = _firstIdx.get(_rec.uuid) ?? -1,\n        _band;\n      if (!_lastBoundary) _band = "live"; // no compaction in file -> the whole chain is the context\n      else if (_live.has(_rec.uuid)) _band = "live"; // authoritative: boundary union preserved\n      else if (_firstAppearance > _lastBoundaryIdx) {\n        // post-boundary by FIRST appearance (dup re-appends fail this)\n        if (_canon.has(_rec.uuid)) _band = "live"; // proven leaf-ancestor -> in resume context\n        // "severed" is a confident NOT-in-context claim, so it needs BOTH a clean walk to\n        // the boundary (_postReliable) AND an authoritative live set (preserved/segment).\n        // In bestEffort mode _live is only the boundary uuid and _canon follows the\n        // max-index leaf, which can be the wrong branch in a multi-branch corpus; a\n        // "severed" there would paint a confident red on maybe-live content, so we\n        // downgrade to "uncertain" and let the banner\'s best-effort hedge stand.\n        else if (_postReliable && _bandMode !== "bestEffort") _band = "severed"; // proven post-compaction orphan -> NOT in context\n        else _band = "uncertain"; // walk unreliable or best-effort live set -> may-or-may-not be in context\n      } else _band = "summarized"; // pre-boundary, non-preserved -> condensed into the summary\n      _rec.__pfgkBand = _band;\n      if (_band === "live") { _liveCount++; _liveUuids.push(_rec.uuid); }\n      else if (_band === "summarized") _summarizedCount++;\n      else if (_band === "severed") { _severedCount++; _severedUuids.push(_rec.uuid); }\n      else { _uncertainCount++; _uncertainUuids.push(_rec.uuid); }\n    }\n    // ----- resplice spine: every unique renderable main-line turn, chronological\n    //       (first-occurrence) order. Drop tool-result carriers (_pv_o1e reattaches\n    //       them) and the planted pfgk- ghosts (a "crossed cleanly" card inside a\n    //       damaged render is contradictory, and one of them caused the cycle).\n    //       Content is last-wins because it comes from _byUuid. -----\n    let _spine = [..._byUuid.values()].filter((_rec) => $pfg.isMain(_rec) && !_pv_s1e(_rec) && !$pfg.isGhost(_rec));\n    _spine.sort((_recA, _recB) => (_firstIdx.get(_recA.uuid) ?? 2e9) - (_firstIdx.get(_recB.uuid) ?? 2e9));\n    _renderOrder = _spine;\n    _renderOrderUuids = new Set(_renderOrder.map((_rec) => _rec.uuid));\n    _term = null; // replace broken walk; suppress healthy bookend/broken verdict\n    _pfBan = {\n      shown: _liveCount + _summarizedCount + _severedCount + _uncertainCount,\n      live: _liveCount,\n      sum: _summarizedCount,\n      sev: _severedCount,\n      unc: _uncertainCount,\n      mode: _bandMode,\n      cyc: _cycleHit,\n      sid: _leaf.sessionId,\n      leaf: _leaf.uuid,\n      ts: (_lastBoundary && _lastBoundary.timestamp) || (_term0 && _term0.timestamp),\n      lU: _liveUuids,\n      sU: _severedUuids,\n      uU: _uncertainUuids,\n    };\n  } else _renderOrder.reverse(); // healthy path only: leaf->root walk into chronological order\n\n  await new Promise((_resolve) => setImmediate(_resolve));\n  let _ren = _pv_o1e(_byUuid, _renderOrder, _renderOrderUuids);\n\n  // The ONE canonical root-outcome failure marker (docs/invariant.md). Loud,\n  // final, and honestly scoped: "not found in what we searched", never the\n  // unproven universal "unrecoverable". Both failure paths route here so the\n  // verdict renders identically, and it stays SEPARATE from the render-mode\n  // CHAIN CORRUPT banner (two orthogonal facts, two strings).\n  let _rootNotFound = ({ uuid, sessionId, timestamp, rows, detail, tm }) => ({\n    type: "user",\n    uuid,\n    parentUuid: null,\n    logicalParentUuid: null,\n    sessionId,\n    timestamp,\n    message: {\n      role: "user",\n      content: $pfg.PFGK1_PREFIX + JSON.stringify({\n        kind: "broken",\n        badge: "⛔ TRANSCRIPT INCOMPLETE · Conversation root not found",\n        glyph: "⛔",\n        headline: "Conversation root not found in the saved files",\n        rows,\n        body:\n          detail +\n          " The backfill searched the other .jsonl files in this session’s own folder and did not find the root there. It may still exist in an archived or earlier session that was not searched.",\n        tm: tm || "",\n      }),\n    },\n  });\n\n  // Wall-clock timing string, shown on EVERY terminal card (green bookend, the loud\n  // root-not-found marker, and the CHAIN CORRUPT banner) so the reconstruction cost is\n  // visible whatever the outcome, and arguably most useful on the failure paths\n  // ("searched N files for X ms, still no root"). Display-only (invariant.md: Date.now\n  // may feed timing telemetry, never the record set or order). Computed here, ABOVE the\n  // corrupt/healthy fork, so both branches reference the one string.\n  let _tmStr =\n    _telemetry && _telemetry.timing\n      ? "K stitching wall-clock: parse " + _telemetry.timing.parseMs + "ms · cross-file " + _telemetry.timing.crossFileMs + "ms · sibling " + _telemetry.timing.siblingBackfillMs + "ms · bookend " + _telemetry.timing.bookendMs + "ms"\n      : "";\n\n  if (Array.isArray(_ren) && _corrupt) {\n    // ----- render-mode banner (orthogonal to the root verdict). No success language. -----\n    let _rows = [\n      ["messages shown", String(_pfBan.shown)],\n      ["in resume context", String(_pfBan.live)],\n      ["summarized away", String(_pfBan.sum)],\n    ];\n    if (_pfBan.sev > 0) _rows.push(["severed (not in context)", String(_pfBan.sev)]);\n    if (_pfBan.unc > 0) _rows.push(["context uncertain", String(_pfBan.unc)]);\n    _rows.push(["damage", _pfBan.cyc ? "cyclic chain" : "stranded history"]);\n    let _body =\n      "The saved conversation chain is corrupt" +\n      (_pfBan.cyc\n        ? " (a parent link loops back into already-seen messages)"\n        : " (messages saved on disk are unreachable from the latest turn)") +\n      ", so the normal lineage walk could not show the whole transcript. Every saved message is shown here in write order. Solid-gutter messages are in the model\'s context if you resume; dimmed messages were condensed into the compaction summary and are not in context." +\n      (_pfBan.sev > 0 || _pfBan.unc > 0\n        ? " Red/amber messages sit after the compaction but are not reachable as context."\n        : "") +\n      (_pfBan.mode !== "preserved"\n        ? " (The in-context split is best-effort: this compaction has no preserved-message list.)"\n        : "");\n    _ren.unshift({\n      type: "user",\n      uuid: $pfg.GHOST_PREFIX + "resplice-" + _pfBan.leaf,\n      parentUuid: null,\n      logicalParentUuid: null,\n      sessionId: _pfBan.sid,\n      timestamp: _pfBan.ts,\n      message: {\n        role: "user",\n        content: $pfg.PFGK1_PREFIX + JSON.stringify({\n          kind: "resplice",\n          badge: "⚠️ CHAIN CORRUPT · RESPLICED",\n          glyph: "⚠️",\n          headline: "Transcript respliced · chain corruption",\n          rows: _rows,\n          body: _body,\n          bands: { live: _pfBan.lU, sev: _pfBan.sU, unc: _pfBan.uU },\n          tm: _tmStr,\n        }),\n      },\n    });\n    // ----- HARD-FAILURE invariant. The verdict is about the LIVE leaf, not the\n    //       oldest resplice turn: if the walk from the latest turn did not reach\n    //       an origin by a verified path (_reachedRoot), fire the loud marker on\n    //       top. Keying on _renderOrder[0] here let a disjoint side-tree\'s origin suppress the\n    //       marker while the live lineage dead-ended (BREACH 1). -----\n    if (!_reachedRoot) {\n      _ren.unshift(\n        _rootNotFound({\n          uuid: $pfg.GHOST_PREFIX + "broken-" + (_term0 ? _term0.uuid : _leaf.uuid),\n          sessionId: _pfBan.sid,\n          timestamp: _pfBan.ts,\n          rows: [\n            ["latest turn", String(_leaf.uuid).slice(0, 8)],\n            _crossedUnprovenSeam\n              ? ["reattached by position", "root unverified"]\n              : ["walk reached", _term0 ? String(_term0.uuid).slice(0, 8) + " (" + _term0.type + ")" : "none"],\n            ["root reached", "NO"],\n            // parity with the healthy-path marker: name the off-disk fork source here too.\n            ...(_rootCls.reason === "fork" && _term0 && _term0.forkedFrom\n              ? [["forked from", String(_term0.forkedFrom.sessionId).slice(0, 8) + " (off-disk)"]]\n              : []),\n          ],\n          detail: _failDetail("oldest message the latest turn traces back to"),\n          tm: _tmStr,\n        }),\n      );\n    }\n  } else if (Array.isArray(_ren) && _term) {\n    // ----- healthy path. The root verdict (_reachedRoot) rests on in-band\n    //       detection (classifyRoot reads the message text, since the vendor\n    //       gives no out-of-band flag for a resume summary; guarded by\n    //       isContinuationPreamble\'s string match) and is reattachment-aware: a\n    //       preamble terminus or a crossed K2 seam is a hard failure (loud\n    //       marker), NOT a green "reconstructed" bookend. -----\n    let _root = _reachedRoot;\n    if (!_root) {\n      _ren.unshift(\n        _rootNotFound({\n          uuid: $pfg.GHOST_PREFIX + "broken-" + _term.uuid,\n          sessionId: _term.sessionId,\n          timestamp: _term.timestamp,\n          rows: [\n            ["walk terminus", String(_term.uuid).slice(0, 8) + " (" + _term.type + ")"],\n            ["parentUuid", _term.parentUuid || "none"],\n            ["lpu", _term.logicalParentUuid || "none"],\n            ...(_crossedUnprovenSeam ? [["chain reattached", "by in-file position"]] : []),\n            ["siblings examined", String(_telemetry ? _telemetry.siblingsScanned : 0)],\n            ["phantoms backfilled", String(_telemetry ? _telemetry.phantomsBackfilled : 0)],\n            ...(_rootCls.reason === "fork" ? [["forked from", String(_term.forkedFrom.sessionId).slice(0, 8) + " (off-disk)"]] : []),\n          ],\n          detail: _failDetail("deepest saved message"),\n          tm: _tmStr,\n        }),\n      );\n    } else if (_recovered) {\n      // The origin, reached across a fork/compaction: the green bookend. Only\n      // fires now when _root is true, so it can never sit on a preamble.\n      let _xfSrc = _telemetry && _telemetry.provBasenames ? _telemetry.provBasenames[_term.uuid] : null;\n      let _bkRows = _xfSrc\n        ? [\n            ["walk terminus", String(_term.uuid).slice(0, 8)],\n            ["source sibling", _xfSrc],\n            ...(_telemetry && _telemetry.prependedMessages > 0 ? [["msgs prepended", String(_telemetry.prependedMessages)]] : []),\n            ...(_telemetry && _telemetry.phantomsBackfilled + _telemetry.phantomsCouldNotBackfill > 0\n              ? [["phantoms backfilled", _telemetry.phantomsBackfilled + " of " + (_telemetry.phantomsBackfilled + _telemetry.phantomsCouldNotBackfill)]]\n              : []),\n          ]\n        : [\n            ["bridged across", "in-file compaction(s)"],\n            ["walk terminus", String(_term.uuid).slice(0, 8)],\n          ];\n      let _bkHead = _xfSrc\n        ? "Conversation origin · reconstructed from sibling fork"\n        : "Conversation origin · reconstructed in-file";\n      let _bkBody = _xfSrc\n        ? "Patch K’s cross-conversation backfill found a sibling fork that retained the conversation’s pre-compaction content. The canonical chain root has been restored at the top of this transcript."\n        : "The walk bridged back across one or more in-file compactions to a legitimate root, so the view is complete.";\n      _ren.unshift({\n        type: "user",\n        uuid: $pfg.GHOST_PREFIX + "bookend-" + _term.uuid,\n        parentUuid: null,\n        logicalParentUuid: null,\n        sessionId: _term.sessionId,\n        timestamp: _term.timestamp,\n        message: { role: "user", content: $pfg.PFGK1_PREFIX + JSON.stringify({ kind: "bookend", headline: _bkHead, rows: _bkRows, body: _bkBody, tm: _tmStr }) },\n      });\n    }\n  }\n  return _ren;\n}\nfunction IA(e){if(e.type!=="assistant")r', 'expected_count': 1}, {'old': 'Meta)return!1;if(e.isSidechain)return!1;if(e.teamName)return!1;return!0}function c1e(e,t){return{type:e', 'new': 'Meta)return!1;if(e.isSidechain)return!1;/*pfg-v1.11*/return!0}function c1e(e,t){return{type:e', 'expected_count': 1}, {'old': 'uid==="string")r.push(n)}return GY(r,t)}async function GY(e,t){let r=await i1e(e),i=t?.includeSystemMessages??!1,n=r.fil', 'new': 'uid==="string")r.push(n)}return GY(r,t)}/*pfg-v1.11*/async function GY(e,t,_tel){let r=await i1e(e,_tel),i=t?.includeSystemMessages??!1,n=r.fil', 'expected_count': 1}, {'old': '(s,i)).map((s)=>c1e(s));return l1e(n,t)}async function d1e(e,t){if(!Nk(e))return[];let r=await qAe(e,t?.dir);if(!r)return[];let i=await r1e(r.filePath,r.fileSize);if(!i)return[];return GY(await n1e(i),t)}function VY(e,t,r){let{head:i,tail:n,mti', 'new': '(s,i)).map((s)=>c1e(s));return l1e(n,t)}/*pfg-v1.11*//*\n * d1e: the session loader (Patches J + K). Replaces the vendor loader body\n * wholesale. Readable source; the apply step injects it into the bundle after\n * the $pfg block (src/pfg-core.js via util/pfg-codegen.py).\n *\n * It parses the session .jsonl, then, before handing the records to the\n * chain-builder, recovers any lineage a compaction severed:\n *   J   cross-file fixed-point backfill: a boundary whose logical parent does\n *       not reach a root in-file gets its pre-compaction history\n *       prepended from the sibling that contributes the most new records (the\n *       deepest ancestor), repeated until nothing dangles or nothing new adds.\n *   K1  phantom backfill: a boundary whose logical parent is on NO record gets\n *       its pre-boundary content prepended from a sibling that DOES carry it.\n *   K2  seam ghost / K3 seamClean+bridge ghost: planted markers showing where a\n *       compaction was crossed and how.\n *\n * SSOT: every "is this a boundary / a root / a ghost / which edge" decision goes\n * through $pfg (src/pfg-core.js), never re-decided inline.\n *\n * Vendor deps are referenced through the _pv_ namespace so the apply driver can\n * DERIVE the set by grepping /_pv_\\w+/ and bind each to the current minified name\n * via a discovered alias prologue. Each dep\'s identity (its meaning, kind, and\n * per-target locator) lives ONCE, in the anchor registry (pfg/anchors.py). This\n * file names deps abstractly and does NOT restate their meaning, so the core and\n * the registry cannot silently drift apart. Restating the semantics here would be\n * the same hand-maintained-double disease the _pv_ grep exists to kill.\n */\nasync function d1e(_session, _opts) {\n  if (!_pv_Nk(_session)) return [];\n  let _meta = await _pv_qAe(_session, _opts?.dir);\n  if (!_meta) return [];\n  let _buf = await _pv_r1e(_meta.filePath, _meta.fileSize);\n  if (!_buf) return [];\n\n  let _tStart = Date.now();\n  let _parsed = await _pv_n1e(_buf);\n  let _tParsed = Date.now();\n\n  // Uuids present so far. _seen grows as backfill prepends sibling records;\n  // _inFileUuids is frozen at the original file\'s contents (for provenance).\n  let _seen = new Set(_parsed.map((_rec) => _rec.uuid));\n  let _inFileUuids = new Set(_seen);\n\n  let _dir = _pv_zn.dirname(_meta.filePath);\n  // Determinism: the reconstruction must be a pure function of the input corpus\n  // bytes. readdir order is filesystem-dependent and file mtime is not an input\n  // at all, so neither may drive the ancestor pick. Sort by filename, so the same\n  // set of .jsonl files yields the same sibling order, and the same pick, on any\n  // machine.\n  let _entries = (await _pv_MY.readdir(_dir)).sort();\n  let _filesParsed = new Map(); // sibling path -> parsed records (parse once)\n  let _bufCache = new Map();    // sibling path -> raw Buffer\n  let _strCache = new Map();    // sibling path -> utf-8 string (cheap uuid scan)\n  let _jPrepended = 0;\n  // Lpus J backfilled where MORE THAN ONE sibling carried a DIVERGENT pre-compaction\n  // history (one holds a record another lacks): the volume-based pick is then one of\n  // several possible reconstructions, so the K3 bridge ghost must disclose it. Mirrors\n  // K1\'s _ambiguousPhantomLpus for phantoms; without it J could splice foreign fork history into\n  // a confident unmarked "reconstructed" render.\n  let _ambiguousJLpus = new Set();\n  let _preservedCyclesRepaired = 0;\n\n  // ---- reconstruction pipeline. The stages below run in a required order; each\n  // ---- consumes the mutated _parsed the prior stages produced, so reordering them changes\n  // ---- the result or reintroduces a bug:\n  // ----   F (fork backfill) + J (sibling backfill) run as a JOINT fixed point (a J-revealed\n  // ----     fork origin must be F-investigated and vice versa, so they iterate until _parsed\n  // ----     stops growing) BEFORE P/K, or K would fire on a phantom a sibling still holds.\n  // ----   P repairs only the strict lpu-into-preserved-tail cycle after siblings have had\n  // ----     their chance to supply the real ancestry, and before K classifies the boundary.\n  // ----   K1 (phantom backfill) BEFORE K2 (seam) BEFORE K3 (bridge): K1 may resolve a phantom\n  // ----     from a sibling (no seam); only a phantom still absent after K1 becomes a K2 seam;\n  // ----     K3 only decorates a boundary whose lpu is now present.\n  // ----   Divergent-edge resolution runs LAST (after K), over the fully assembled set, so it\n  // ----     resolves the final edges the render walks, not an intermediate state.\n\n  // ---- F: fork source backfill (forkedFrom, investigate-don\'t-guess) ------\n  // A structural origin carrying forkedFrom is a COPY of a turn from a source session\n  // (a /branch). If the branch was taken mid-conversation, the source holds the turns\n  // BEFORE the fork point; without them this origin masquerades as the root (the\n  // fork axis of the false-success). Investigate rather than guess: read the source and,\n  //   - if it holds CANONICAL ancestry (a main-line turn before the copy), prepend it and\n  //     re-root the copy onto its real source parent, so the walk reaches the SOURCE\'s own\n  //     origin and the normal green/marker verdict applies;\n  //   - if the copy has NO canonical turn before it in the source (empty / system-only\n  //     pre-fork region), the fork is COMPLETE and this copy IS the origin: flag it green;\n  //   - if the source is off-disk we cannot investigate: leave the forkedFrom in place and\n  //     i1e fires the LOUD MARKER (undecidable off-disk is a hard failure, not a soft note).\n  // Fixed-point: a source that is itself a fork recurses. Guards `__pfgkForkChecked`\n  // so each copy is investigated once. 0-observed in the corpus (its forks are\n  // compacted-boundary heads), the honest guard for the shape rather than a hedge.\n  // Runs to its own fixed point. The joint loop below re-runs it after J, because a\n  // J sibling-prepend can surface a fork origin this then has to resolve.\n  async function _backfillForkSources() {\n    for (let _pass = 0; _pass < 100000; _pass++) {\n      let _madeProgress = false;\n      for (let _copy of _parsed) {\n        if (!$pfg.isOrigin(_copy) || !_copy.forkedFrom || !_copy.forkedFrom.sessionId || _copy.__pfgkForkChecked) continue;\n        let _sourcePath = _pv_zn.join(_dir, _copy.forkedFrom.sessionId + ".jsonl");\n        if (_sourcePath === _meta.filePath) { _copy.__pfgkForkChecked = true; continue; }\n        let _sourceMsgs = _filesParsed.get(_sourcePath);\n        if (!_sourceMsgs) {\n          try {\n            let _sourceBuf = await _pv_MY.readFile(_sourcePath);\n            _sourceMsgs = await _pv_n1e(_sourceBuf);\n            _filesParsed.set(_sourcePath, _sourceMsgs);\n          } catch { _copy.__pfgkForkChecked = true; continue; } // off-disk: undecidable, leave for the marker\n        }\n        // Key the source lookup on the ATTESTED forkedFrom.messageUuid (fall back to the\n        // copy\'s own uuid, identical on all real data), so a branch copy that ever gets a\n        // fresh uuid is still found in the source rather than silently no-op\'ing to a marker.\n        let _forkIdx = -1, _forkPointUuid = _copy.forkedFrom.messageUuid || _copy.uuid;\n        for (let _i = 0; _i < _sourceMsgs.length; _i++)\n          if (_sourceMsgs[_i].uuid === _forkPointUuid) { _forkIdx = _i; break; }\n        _copy.__pfgkForkChecked = true;\n        if (_forkIdx < 0) continue; // copy absent from source: leave for the marker\n        // Complete fork iff the source\'s fork point is ITSELF a genuine origin\n        // (classifyRoot reachedRoot): a real first prompt, with NO ancestry before it,\n        // not a preamble, not itself a fork. The old isMain-position scan missed a\n        // compact_boundary before the fork point (non-isMain, yet off-disk ancestry via\n        // its lpu), wrongly flagging a boundary-headed source complete: a false success,\n        // the fork analogue of the preamble trap (docs/invariant.md).\n        // (Branch at the source\'s very start is the _forkIdx === 0 origin case.)\n        let _forkPointVerdict = $pfg.classifyRoot(_sourceMsgs[_forkIdx]);\n        if (_forkPointVerdict.reachedRoot) { _copy.__pfgkForkComplete = true; continue; }\n        // NESTED fork AT the fork point itself (the _forkIdx === 0 sub-case the prepend\n        // cannot reach: the source\'s fork point is its own fork origin, sharing this copy\'s\n        // uuid). Chain: redirect this copy at the grand-source and re-investigate next pass,\n        // so an N-level on-disk fork chain resolves to the real origin instead of a premature\n        // marker (forbidden-middle #2). Terminates: each hop consumes a distinct on-disk\n        // source. (A fork ancestor one level DOWN, _forkIdx > 0, is already handled by the\n        // prepend below, which prepends the source\'s own fork origin for the next pass.)\n        if (_forkPointVerdict.reason === "fork") {\n          _copy.forkedFrom = _sourceMsgs[_forkIdx].forkedFrom;\n          _copy.__pfgkForkChecked = false;\n          _madeProgress = true;\n          continue;\n        }\n        // else (none / preamble): the fork point has ancestry (a canonical parent or a\n        // boundary lpu). Prepend the source\'s pre-fork ancestry and re-root; i1e fires the\n        // marker if that ancestry is off disk.\n        let _preForkAncestry = [];\n        for (let _i = 0; _i < _forkIdx; _i++) {\n          let _rec = _sourceMsgs[_i];\n          if (_rec && _rec.uuid && !_seen.has(_rec.uuid)) { _preForkAncestry.push(_rec); _seen.add(_rec.uuid); }\n        }\n        _copy.parentUuid = _sourceMsgs[_forkIdx].parentUuid ?? null; // re-root onto the real source parent\n        _copy.logicalParentUuid = _sourceMsgs[_forkIdx].logicalParentUuid ?? _copy.logicalParentUuid ?? null;\n        if (_preForkAncestry.length > 0) _parsed = [..._preForkAncestry, ..._parsed];\n        _madeProgress = true;\n      }\n      if (!_madeProgress) break;\n    }\n  }\n\n  // ---- J: cross-file fixed-point backfill --------------------------------\n  // A boundary "dangles" when its logical parent does not reach an origin\n  // within the records we hold. Not merely "lpu absent": a resume imports the\n  // parent\'s post-compaction working set INTO the child, so the lpu target can be\n  // present yet its chain still truncated. $pfg.walkToRoot follows the real edge\n  // (parent, else logical parent) and reads content, so a continuation preamble\n  // does NOT count as a reached root and the search keeps going past it, exactly\n  // as the invariant requires.\n  // J iterates to a FIXED POINT: each pass prepends the deepest new ancestor and\n  // may reveal a deeper dangling boundary, so the pass count is the compaction-chain\n  // DEPTH, not a constant. It terminates because every continuing pass adds at least\n  // one new record to _seen (the _newPrepend.length===0 break) over a finite corpus,\n  // and sibling reads are cached. The high bound is a pure infinite-loop backstop: a\n  // small fixed cap could cut a still-progressing deep recovery and fire the loud\n  // marker on a recoverable chain (invariant forbidden-middle #2), so it must sit far\n  // above any real chain depth rather than at a hand-picked number.\n  async function _backfillSiblings() {\n    for (let _pass = 0; _pass < 100000; _pass++) {\n      let _index = $pfg.byUuid(_parsed);\n      let _dangling = [];\n      for (let _rec of _parsed)\n        if ($pfg.isBoundary(_rec) && !_rec.parentUuid && _rec.logicalParentUuid) {\n          let _boundaryLpu = _rec.logicalParentUuid;\n          if (!_seen.has(_boundaryLpu) || !$pfg.walkToRoot(_boundaryLpu, _index).reachedRoot)\n            _dangling.push(_boundaryLpu);\n        }\n      if (_dangling.length === 0) break;\n\n      // For each dangling lpu, pick the sibling whose [0..idx(lpu)] prefix adds the\n      // MOST not-yet-seen records: the deepest ancestor, not just the first match.\n      let _deepestByFile = new Map();\n      for (let _lpu of new Set(_dangling)) {\n        let _bestPath = null, _bestIdx = -1, _mostNew = 0, _candidatePreSets = [], _bestPreSet = null;\n        for (let _name of _entries) {\n          if (!_name.endsWith(".jsonl")) continue;\n          let _path = _pv_zn.join(_dir, _name);\n          if (_path === _meta.filePath) continue;\n          let _siblingMsgs = _filesParsed.get(_path);\n          if (!_siblingMsgs) {\n            let _siblingText = _strCache.get(_path);\n            let _siblingBuf = _bufCache.get(_path);\n            if (_siblingText === void 0) {\n              _siblingBuf = await _pv_MY.readFile(_path);\n              _bufCache.set(_path, _siblingBuf);\n              _siblingText = _siblingBuf.toString("utf-8");\n              _strCache.set(_path, _siblingText);\n            }\n            if (!_siblingText.includes(`"uuid":"${_lpu}"`)) continue;\n            _siblingMsgs = await _pv_n1e(_siblingBuf);\n            _filesParsed.set(_path, _siblingMsgs);\n          }\n          let _lpuIdx = -1;\n          for (let _i = 0; _i < _siblingMsgs.length; _i++)\n            if (_siblingMsgs[_i].uuid === _lpu) { _lpuIdx = _i; break; }\n          if (_lpuIdx === -1) continue;\n          let _newCount = 0, _preSet = new Set();\n          for (let _i = 0; _i <= _lpuIdx; _i++) {\n            let _rec = _siblingMsgs[_i];\n            if (_rec && _rec.uuid) { _preSet.add(_rec.uuid); if (!_seen.has(_rec.uuid)) _newCount++; }\n          }\n          _candidatePreSets.push(_preSet);\n          if (_newCount > _mostNew) { _mostNew = _newCount; _bestPath = _path; _bestIdx = _lpuIdx; _bestPreSet = _preSet; }\n        }\n        // Ambiguous iff >1 sibling carries this lpu AND the winner does NOT subsume the\n        // others: a losing sibling holds a pre-boundary record the winner lacks, i.e. a\n        // DIVERGENT fork, so the volume pick is one of several reconstructions. Nested\n        // candidates (the winner IS the superset) are not ambiguous. Mirrors K1\'s\n        // _ambiguousPhantomLpus; the K3 bridge ghost discloses it.\n        if (_candidatePreSets.length > 1 && _bestPreSet) {\n          let _union = new Set();\n          for (let _set of _candidatePreSets) for (let _uuid of _set) _union.add(_uuid);\n          if (_bestPreSet.size < _union.size) _ambiguousJLpus.add(_lpu);\n        }\n        if (_bestPath) {\n          let _prev = _deepestByFile.get(_bestPath);\n          if (_prev === void 0 || _bestIdx > _prev) _deepestByFile.set(_bestPath, _bestIdx);\n        }\n      }\n      if (_deepestByFile.size === 0) break;\n\n      let _newPrepend = [];\n      for (let [_path, _deepestIdx] of _deepestByFile) {\n        let _siblingMsgs = _filesParsed.get(_path);\n        for (let _i = 0; _i <= _deepestIdx; _i++) {\n          let _rec = _siblingMsgs[_i];\n          if (_rec && !_seen.has(_rec.uuid)) { _newPrepend.push(_rec); _seen.add(_rec.uuid); }\n        }\n      }\n      if (_newPrepend.length === 0) break;\n      _jPrepended += _newPrepend.length;\n      _parsed = [..._newPrepend, ..._parsed];\n    }\n  }\n\n  // ---- F + J JOINT fixed point ----------------------------------------------\n  // F is UPSTREAM of J: a fork origin that surfaces only via a J sibling-prepend (the\n  // backfill sibling\'s OWN origin is a fork copy) would never be F-investigated in one\n  // F-then-J sequence, firing a premature marker even with the grand-source on disk\n  // (invariant forbidden-middle #2). Re-run F then J until a full round adds no record:\n  // J reveals F work (a prepended fork origin) and F\'s prepend reveals J work (a\n  // dangling boundary J then catches), so a single re-run of either is not enough. Any\n  // forkedFrom still unresolved once a round is empty is genuinely off disk and\n  // correctly marks. The high bound is a pure infinite-loop backstop; each real round\n  // adds at least one record over a finite corpus.\n  for (let _outer = 0; _outer < 100000; _outer++) {\n    let _beforeLen = _parsed.length;\n    await _backfillForkSources();\n    await _backfillSiblings();\n    if (_parsed.length === _beforeLen) break;\n  }\n\n  // ---- P: preserved-tail cycle repair ---------------------------------------\n  // One compaction shape reuses the preserved-window records under the new compact\n  // summary while the boundary\'s logicalParentUuid still points at that window\'s tail:\n  //\n  //   boundary --lpu--> tail -> ... -> head -> summary -> boundary\n  //\n  // The target uuid therefore EXISTS, but its walk cycles. J correctly searches\n  // siblings because it does not reach an origin; K2 then ignores it because it is\n  // not phantom, and K3 used to call it seamClean before i1e finally detected the\n  // cycle. The compaction metadata carries enough information for a narrow repair:\n  // the complete preserved order and, when the boundary is not first in the file,\n  // the immediately preceding in-file record. Rebuild that window onto the predecessor\n  // and put the marker on the repaired canonical path. Do not generalise this to every\n  // cycle: all predicates below describe the measured lpu-into-preserved-tail shape.\n  {\n    let _index = $pfg.byUuid(_parsed);\n    for (let _i = 0; _i < _parsed.length; _i++) {\n      let _boundary = _parsed[_i];\n      if (!$pfg.isBoundary(_boundary) || _boundary.parentUuid != null || !_boundary.logicalParentUuid) continue;\n      let _preserved = _boundary.compactMetadata?.preservedMessages,\n        _segment = _boundary.compactMetadata?.preservedSegment,\n        _uuids = _preserved?.uuids;\n      if (!Array.isArray(_uuids) || _uuids.length === 0 || _uuids.some((_uuid) => !_index.has(_uuid))) continue;\n      let _lpu = _boundary.logicalParentUuid,\n        _headUuid = _uuids[0],\n        _tailUuid = _uuids.at(-1);\n      if (_lpu !== _tailUuid || (_segment?.tailUuid != null && _segment.tailUuid !== _tailUuid)) continue;\n      let _lpuWalk = $pfg.walkToRoot(_lpu, _index);\n      if (_lpuWalk.reason !== "cycle" || !_lpuWalk.path.some((_rec) => _rec.uuid === _boundary.uuid)) continue;\n\n      let _window = new Set(_uuids), _predUuid = null;\n      for (let _j = _i - 1; _j >= 0; _j--) {\n        let _uuid = _parsed[_j]?.uuid;\n        if (_uuid && _inFileUuids.has(_uuid) && !_window.has(_uuid)) { _predUuid = _uuid; break; }\n      }\n      if (!_predUuid) continue; // boundary-at-file-start still needs J/K1/K2 evidence\n\n      // The positional connection is trusted only when the predecessor reaches the\n      // file\'s one fork-free origin without crossing a foreign fork or unverified seam.\n      let _ownOrigins = _parsed.filter((_rec) =>\n          $pfg.isOrigin(_rec) && !(_rec.forkedFrom && _rec.forkedFrom.sessionId)),\n        _predWalk = $pfg.walkToRoot(_predUuid, _index),\n        _proven =\n          _ownOrigins.length === 1 &&\n          _predWalk.reachedRoot &&\n          !!_predWalk.terminus &&\n          _predWalk.terminus.uuid === _ownOrigins[0].uuid &&\n          !_predWalk.path.some((_rec) => _rec.forkedFrom && _rec.forkedFrom.sessionId) &&\n          !$pfg.crossedUnprovenSeam(_predWalk.path),\n        _kind = _proven ? "seamClean" : "seam",\n        _ghostUuid = $pfg.GHOST_PREFIX + _kind + "-" + _boundary.uuid.slice(0, 8);\n      if (_seen.has(_ghostUuid)) continue;\n\n      let _payload = _proven\n        ? {\n            kind: "seamClean",\n            badge: "◇ IN-FILE COMPACTION",\n            headline: "Compaction event · preserved window repaired in-file",\n            rows: [\n              ["boundary", String(_boundary.uuid).slice(0, 8)],\n              ["preserved messages", String(_uuids.length)],\n              ["reconnected to", String(_predUuid).slice(0, 8)],\n            ],\n            body:\n              "The compactor placed its preserved messages under the compact summary while the boundary still pointed at their tail, creating a cycle. Patch K restored the recorded preserved-message order and reconnected its head to the preceding in-file turn.",\n          }\n        : {\n            kind: "seam",\n            rows: [\n              ["cyclic preserved tail", String(_lpu).slice(0, 8) + " ↻"],\n              ["reattached to", String(_predUuid)],\n              ["preserved messages", String(_uuids.length)],\n            ],\n            body:\n              "The compactor placed its preserved messages under the compact summary while the boundary still pointed at their tail, creating a cycle. Patch K restored the recorded preserved-message order and reattached its head to the preceding in-file record, but that positional predecessor did not prove one unambiguous origin.",\n          };\n      let _ghost = {\n        type: "user",\n        uuid: _ghostUuid,\n        parentUuid: _predUuid,\n        sessionId: _boundary.sessionId,\n        timestamp: _boundary.timestamp,\n        message: { role: "user", content: $pfg.PFGK1_PREFIX + JSON.stringify(_payload) },\n        ...(!_proven ? { __pfgkSeam: { reattachTarget: _predUuid, proven: false } } : {}),\n      };\n\n      // Write one parent choice onto every occurrence of each preserved uuid. d1e\'s\n      // later divergent-edge pass then sees no stale alternative to select last-wins.\n      let _parentCursor = _ghostUuid;\n      for (let _uuid of _uuids) {\n        for (let _rec of _parsed) if (_rec.uuid === _uuid) _rec.parentUuid = _parentCursor;\n        _parentCursor = _uuid;\n      }\n      _boundary.__pfgkPreservedCycleRepair = {\n        headUuid: _headUuid,\n        tailUuid: _tailUuid,\n        predecessorUuid: _predUuid,\n        proven: _proven,\n      };\n      _parsed.splice(_i, 0, _ghost);\n      _seen.add(_ghostUuid);\n      _preservedCyclesRepaired++;\n      _i++;\n      _index = $pfg.byUuid(_parsed);\n    }\n  }\n\n  // Provenance: which file each uuid came from (in-file first, then siblings).\n  let _prov = new Map();\n  for (let _uuid of _inFileUuids) _prov.set(_uuid, _meta.filePath);\n  if (_filesParsed)\n    for (let [_siblingPath, _siblingMsgs] of _filesParsed)\n      for (let _rec of _siblingMsgs)\n        if (_rec.uuid && !_prov.has(_rec.uuid)) _prov.set(_rec.uuid, _siblingPath);\n  let _tAfterBackfill = Date.now();\n\n  // ---- K1: phantom backfill ---------------------------------------------\n  // A boundary whose logical parent is on no record we hold. Find a sibling that\n  // carries that lpu as ITS boundary and prepend the sibling\'s pre-boundary\n  // content, validated: the sibling must have a real turn before the boundary\n  // and an origin of its own.\n  let _ambiguousPhantomLpus = new Set();\n  let _k1Sources = new Map();\n  let _phantomTotal = 0;\n  {\n    let _phantomLpus = new Set();\n    for (let _rec of _parsed)\n      if ($pfg.isBoundary(_rec) && !_rec.parentUuid && _rec.logicalParentUuid && !_seen.has(_rec.logicalParentUuid))\n        _phantomLpus.add(_rec.logicalParentUuid);\n    _phantomTotal = _phantomLpus.size;\n    for (let _phantomLpu of _phantomLpus) {\n      let _bestPreBoundary = null, _bestSrcName = null, _qualifyingCount = 0;\n      for (let _name of _entries) {\n        if (!_name.endsWith(".jsonl")) continue;\n        let _path = _pv_zn.join(_dir, _name);\n        if (_path === _meta.filePath) continue;\n        let _siblingMsgs = _filesParsed.get(_path);\n        if (!_siblingMsgs) {\n          try {\n            let _siblingBuf = await _pv_MY.readFile(_path);\n            let _siblingText = _siblingBuf.toString("utf-8");\n            if (!_siblingText.includes(`"logicalParentUuid":"${_phantomLpu}"`)) continue;\n            _siblingMsgs = await _pv_n1e(_siblingBuf);\n            _filesParsed.set(_path, _siblingMsgs);\n          } catch { continue; }\n        } else {\n          let _carriesLpu = false;\n          for (let _sibRec of _siblingMsgs)\n            if ($pfg.isBoundary(_sibRec) && _sibRec.logicalParentUuid === _phantomLpu) { _carriesLpu = true; break; }\n          if (!_carriesLpu) continue;\n        }\n        // Locate the boundary carrying this lpu in the sibling.\n        let _boundaryIdx = -1;\n        for (let _j = 0; _j < _siblingMsgs.length; _j++) {\n          let _sibRec = _siblingMsgs[_j];\n          if ($pfg.isBoundary(_sibRec) && _sibRec.logicalParentUuid === _phantomLpu) { _boundaryIdx = _j; break; }\n        }\n        if (_boundaryIdx <= 0) continue;\n        // The sibling must actually carry pre-boundary conversation...\n        let _hasPreBoundaryTurn = false;\n        for (let _j = 0; _j < _boundaryIdx; _j++)\n          if ($pfg.isMain(_siblingMsgs[_j])) { _hasPreBoundaryTurn = true; break; }\n        if (!_hasPreBoundaryTurn) continue;\n        // ...and have an origin of its own. TIGHTENED to $pfg.isOrigin: the\n        // old inline test accepted "no parent, no lpu, main turn" and so would\n        // treat a compaction summary or a planted ghost as the first child.\n        let _siblingOrigin = null;\n        for (let _j = 0; _j < _siblingMsgs.length; _j++)\n          if ($pfg.isOrigin(_siblingMsgs[_j])) { _siblingOrigin = _siblingMsgs[_j]; break; }\n        if (!_siblingOrigin) continue;\n        _qualifyingCount++;\n        // Collect this candidate\'s not-yet-seen pre-boundary content WITHOUT\n        // mutating the shared _seen (mirror J\'s non-mutating comparison at line ~103).\n        // _localSeen dedups within this one sibling; _seen stays the same baseline for\n        // every candidate, so a LOSING candidate leaves no record marked seen-but-\n        // never-prepended, which would otherwise strand it and orphan the K2/K3 ghosts\n        // and bias the length comparison toward whichever sibling was scanned first.\n        let _preBoundary = [], _localSeen = new Set();\n        for (let _j = 0; _j < _boundaryIdx; _j++) {\n          let _sibRec = _siblingMsgs[_j];\n          if (_sibRec && _sibRec.uuid && !_seen.has(_sibRec.uuid) && !_localSeen.has(_sibRec.uuid)) { _preBoundary.push(_sibRec); _localSeen.add(_sibRec.uuid); }\n        }\n        if (_preBoundary.length === 0) continue;\n        if (!_bestPreBoundary || _preBoundary.length > _bestPreBoundary.length) { _bestPreBoundary = _preBoundary; _bestSrcName = _name; }\n      }\n      if (_bestPreBoundary) {\n        for (let _sibRec of _bestPreBoundary) _seen.add(_sibRec.uuid); // commit ONLY the winner, now the comparison is done\n        _parsed = [..._bestPreBoundary, ..._parsed];\n        _k1Sources.set(_phantomLpu, { src: _bestSrcName, count: _bestPreBoundary.length, candidates: _qualifyingCount });\n      }\n      if (_qualifyingCount > 1) _ambiguousPhantomLpus.add(_phantomLpu);\n    }\n  }\n  let _tAfterK1 = Date.now();\n\n  // K1 may have parsed new sibling files; re-extend provenance so a boundary from\n  // a K1-only sibling is not misread as in-file (L4).\n  if (_filesParsed)\n    for (let [_siblingPath, _siblingMsgs] of _filesParsed)\n      for (let _rec of _siblingMsgs)\n        if (_rec.uuid && !_prov.has(_rec.uuid)) _prov.set(_rec.uuid, _siblingPath);\n\n  // ---- K2: seam ghost for a still-phantom boundary ----------------------\n  // Logical parent still absent from every record: reattach the chain to the\n  // in-file predecessor and plant a seam marker explaining the write-side gap.\n  // Seam-trust evidence (task 28): count the forkedFrom-free own-origins ONCE. A seam\n  // promotes to a trusted bridge (no marker) only when there is EXACTLY ONE, so the\n  // positional reattach cannot land on the wrong own-branch; the multi-origin tail\n  // (~1.7% of files) has >1 and stays a marker. Stable across the loop below: a seam\n  // ghost carries a parentUuid, so it is never itself an origin.\n  let _ownOrigins = _parsed.filter((_rec) => $pfg.isOrigin(_rec) && !(_rec.forkedFrom && _rec.forkedFrom.sessionId));\n  let _uniqueOwnOrigin = _ownOrigins.length === 1 ? _ownOrigins[0].uuid : null;\n  let _kFired = false, _kAttempted = false; // vestigial telemetry hooks; kept as-is\n  for (let _i = 0; _i < _parsed.length; _i++) {\n    let _rec = _parsed[_i];\n    if ($pfg.isBoundary(_rec) && !_rec.parentUuid && _rec.logicalParentUuid && !_seen.has(_rec.logicalParentUuid)) {\n      _kAttempted = true;\n      let _predUuid = null;\n      for (let _j = _i - 1; _j >= 0; _j--)\n        if (_parsed[_j].uuid) { _predUuid = _parsed[_j].uuid; break; }\n      if (!_predUuid) continue;\n      // Does the reattach target PROVABLY rejoin THIS conversation? Only if it walks to the\n      // unique own-origin crossing no forkedFrom copy (foreign lineage) and no other\n      // UNVERIFIED seam (an unproven hop). $pfg.rootTrusted (in i1e) then folds `proven`\n      // across every seam on the live leaf\'s path, so a chain of seams needs all proven.\n      let _proven = false;\n      if (_uniqueOwnOrigin) {\n        let _targetWalk = $pfg.walkToRoot(_predUuid, $pfg.byUuid(_parsed));\n        _proven =\n          _targetWalk.reachedRoot &&\n          !!_targetWalk.terminus &&\n          _targetWalk.terminus.uuid === _uniqueOwnOrigin &&\n          !_targetWalk.path.some((_pathRec) => _pathRec.forkedFrom && _pathRec.forkedFrom.sessionId) &&\n          !_targetWalk.path.some((_pathRec) => $pfg.ghostKind(_pathRec) === "seam");\n      }\n      let _seamUuid = $pfg.GHOST_PREFIX + "seam-" + _rec.uuid.slice(0, 8);\n      if (_seen.has(_seamUuid)) continue; // dedup: two boundaries sharing an 8-hex uuid prefix (mirror K3)\n      let _origLpu = _rec.logicalParentUuid;\n      let _ghostContent = $pfg.PFGK1_PREFIX + JSON.stringify({\n        kind: "seam",\n        rows: [\n          ["missing phantom", String(_origLpu) + " ✗"],\n          ["reattached to", String(_predUuid)],\n          ["bug origin", "compact.ts:598 (write-side)"],\n        ],\n        body:\n          (_ambiguousPhantomLpus && _ambiguousPhantomLpus.has(_origLpu)\n            ? "⚠ AMBIGUOUS RECONSTRUCTION: multiple sibling files qualified for backfill at this compaction event; the prepended pre-compaction content is one of several possible reconstructions. "\n            : "") +\n          "Claude Code compacted the conversation here. The compactor referenced a chain predecessor never persisted to disk (write-side bug, compact.ts:598). Patch K reconnected the chain to the nearest preceding record (earlier in this file, or prepended from a sibling).",\n      });\n      let _ghost = {\n        type: "user",\n        uuid: _seamUuid,\n        parentUuid: _predUuid,\n        sessionId: _rec.sessionId,\n        timestamp: _rec.timestamp,\n        message: { role: "user", content: _ghostContent },\n        // Seam-trust evidence read by $pfg.rootTrusted at render: proven=true promotes this\n        // unverified positional reattach to a trusted bridge (no marker). See the guard above.\n        __pfgkSeam: { reattachTarget: _predUuid, proven: _proven },\n      };\n      _parsed.splice(_i, 0, _ghost);\n      _rec.logicalParentUuid = _seamUuid;\n      _seen.add(_ghost.uuid);\n      _kFired = true;\n      _i++;\n    }\n  }\n\n  // ---- K3: seamClean (in-file) / bridge (cross-file) ghost --------------\n  // Logical parent is present, reaches an origin, and is not itself a ghost: mark\n  // whether the pre-boundary lineage was recovered in-file or from a sibling. Mere\n  // uuid presence is insufficient; a preserved-tail target can exist yet cycle.\n  for (let _i = 0; _i < _parsed.length; _i++) {\n    let _rec = _parsed[_i];\n    if (\n      $pfg.isBoundary(_rec) && !_rec.parentUuid && _rec.logicalParentUuid &&\n      !_rec.__pfgkPreservedCycleRepair &&\n      _seen.has(_rec.logicalParentUuid) && !$pfg.isGhostUuid(_rec.logicalParentUuid) &&\n      $pfg.walkToRoot(_rec.logicalParentUuid, $pfg.byUuid(_parsed)).reachedRoot\n    ) {\n      let _origLpu = _rec.logicalParentUuid;\n      let _lpuFile = _prov.get(_origLpu) || _meta.filePath;\n      let _boundaryFile = _prov.get(_rec.uuid) || _meta.filePath;\n      let _crossFile = _boundaryFile !== _lpuFile;\n      let _crossFileSrc = _crossFile ? _pv_zn.basename(_lpuFile) : null;\n      let _ghostUuid = $pfg.GHOST_PREFIX + (_crossFile ? "bridge-" : "seamClean-") + _rec.uuid.slice(0, 8);\n      if (_seen.has(_ghostUuid)) continue;\n      let _ghostPayload = _crossFile\n        ? {\n            kind: "bridge",\n            badge: "↻ CROSS-FILE BRIDGE",\n            headline: "Compaction origin · bridged from a sibling conversation",\n            rows: [\n              ["phantom (J-resolved)", String(_origLpu).slice(0, 8) + " ✗"],\n              ["cross-file source", _crossFileSrc || "(sibling .jsonl)"],\n              ["boundary", String(_rec.uuid).slice(0, 8)],\n            ],\n            body:\n              (_ambiguousJLpus && _ambiguousJLpus.has(_origLpu)\n                ? "⚠ AMBIGUOUS RECONSTRUCTION: more than one sibling file carries a divergent pre-compaction history for this boundary; the bridged content is one of several possible reconstructions. "\n                : "") +\n              "This compaction’s pre-boundary lineage lives in a sibling .jsonl. Patch J resolved it cross-file and Patch K bridges it in, so the chain renders whole across both files.",\n          }\n        : {\n            kind: "seamClean",\n            badge: "◇ IN-FILE COMPACTION",\n            headline: "Compaction event · crossed in-file",\n            rows: [\n              ["boundary", String(_rec.uuid).slice(0, 8)],\n              ["bridged via", "in-file predecessor"],\n            ],\n            body:\n              "Claude Code compacted the conversation here; the messages above were bridged across this boundary in-file.",\n          };\n      let _ghost = {\n        type: "user",\n        uuid: _ghostUuid,\n        parentUuid: _origLpu,\n        sessionId: _rec.sessionId,\n        timestamp: _rec.timestamp,\n        message: { role: "user", content: $pfg.PFGK1_PREFIX + JSON.stringify(_ghostPayload) },\n      };\n      _parsed.splice(_i, 0, _ghost);\n      _rec.logicalParentUuid = _ghostUuid;\n      _seen.add(_ghost.uuid);\n      _i++;\n    }\n  }\n\n  // ---- Divergent-edge resolution -----------------------------------------\n  // When the same uuid appears with DIFFERENT parentUuids across the assembled\n  // records (a re-append that co-occurs with a backfilled sibling, say), $pfg.byUuid\n  // is last-wins and would keep an ARBITRARY edge. Don\'t guess: try each candidate\n  // edge and keep the one whose chain reaches an origin, the invariant\'s own\n  // criterion applied to the choice. Rare (measured ~0.05% of uuids carry divergent\n  // parents) and cheap, but strictly better than an arbitrary pick when it happens.\n  {\n    let _candidateParents = new Map(); // uuid -> the distinct parentUuids seen for it\n    for (let _rec of _parsed)\n      if (_rec && _rec.uuid) {\n        if (!_candidateParents.has(_rec.uuid)) _candidateParents.set(_rec.uuid, new Set());\n        _candidateParents.get(_rec.uuid).add(_rec.parentUuid ?? null);\n      }\n    let _index = $pfg.byUuid(_parsed);\n    for (let [_uuid, _parents] of _candidateParents) {\n      if (_parents.size <= 1) continue; // no divergence, nothing to resolve\n      let _base = _index.get(_uuid), _pick = null;\n      // deterministic candidate order; keep the FIRST that reaches an origin\n      // codepoint (UTF-16 code-unit) order, NOT String.localeCompare: locale-dependent\n      // sorting would make the divergent-edge candidate pick vary by host locale, breaking\n      // the determinism invariant (same corpus bytes -> same reconstruction everywhere).\n      for (let _parent of [..._parents].sort((_a, _b) => (String(_a) < String(_b) ? -1 : String(_a) > String(_b) ? 1 : 0))) {\n        let _probe = new Map(_index);\n        _probe.set(_uuid, { ..._base, parentUuid: _parent });\n        if ($pfg.walkToRoot(_uuid, _probe).reachedRoot) { _pick = _parent; break; }\n      }\n      if (_pick !== null) for (let _rec of _parsed) if (_rec.uuid === _uuid) _rec.parentUuid = _pick;\n    }\n  }\n\n  let _tDone = Date.now();\n\n  let _telemetry = {\n    timing: {\n      parseMs: _tParsed - _tStart,\n      crossFileMs: _tAfterBackfill - _tParsed,\n      siblingBackfillMs: _tAfterK1 - _tAfterBackfill,\n      bookendMs: _tDone - _tAfterK1,\n    },\n    siblingsScanned: _entries\n      ? _entries.filter((_name) => _name.endsWith(".jsonl") && _pv_zn.join(_dir, _name) !== _meta.filePath).length\n      : 0,\n    phantomsBackfilled: _k1Sources.size,\n    phantomsCouldNotBackfill: Math.max(0, _phantomTotal - _k1Sources.size),\n    preservedCyclesRepaired: _preservedCyclesRepaired,\n    prependedMessages:\n      _jPrepended + (() => { let _sum = 0; for (let [, _srcInfo] of _k1Sources) _sum += _srcInfo.count; return _sum; })(),\n    sources: (() => { let _srcByLpu = {}; for (let [_lpu, _srcInfo] of _k1Sources) _srcByLpu[_lpu] = _srcInfo.src; return _srcByLpu; })(),\n    provBasenames: (() => {\n      let _basenamesByUuid = {};\n      if (_prov) for (let [_uuid, _path] of _prov) if (_path !== _meta.filePath) _basenamesByUuid[_uuid] = _pv_zn.basename(_path);\n      return _basenamesByUuid;\n    })(),\n  };\n  return _pv_GY(_parsed, _opts, _telemetry);\n}\nfunction VY(e,t,r){let{head:i,tail:n,mti', 'expected_count': 1}, {'old': 'rse(d);if(!Number.isNaN(_))p=_}let f=l||Aa(n,"lastPrompt")||Aa(n,"summary")||u;if(!f)return null;let m=Aa(n,"gitBranch', 'new': 'rse(d);if(!Number.isNaN(_))p=_}let f=l||u||Aa(n,"summary")||Aa(n,"lastPrompt");if(!f)return null;let m=Aa(n,"gitBranch', 'expected_count': 1}, {'old': '??So(c,"aiTitle")??So(a,"aiTitle"),m=f||So(c,"lastPrompt")||So(c,"summary")||Mse(a);if(!m)return null;return{lastModified:', 'new': '??So(c,"aiTitle")??So(a,"aiTitle"),m=f||Mse(a)||So(c,"summary")||So(c,"lastPrompt");if(!m)return null;return{lastModified:', 'expected_count': 1}, {'old': 'geId,w);if(l&&b)this.summaries.set(b,l);return this.loadedSessions.add(o),o}}asy', 'new': 'geId,w);if(l&&b)this.summaries.set(b,l);{let _srcCustom="",_srcAi="";try{let _src=(await Qn.promises.readFile(u,"utf8")).split(`\\n`);for(let _line of _src){if(!_line)continue;try{let _M=JSON.parse(_line);if(_M.type==="custom-title"&&_M.customTitle)_srcCustom=_M.customTitle;if(_M.type==="ai-title"&&_M.aiTitle)_srcAi=_M.aiTitle}catch(_){}}}catch(_){}let _srcTitle=_srcCustom||_srcAi;let _lp="",_lpEndBytes=-1,_byteOffset=0;for(let _i=0;_i<m.length;_i++){let _m=m[_i];let _lineBytes=Buffer.byteLength(JSON.stringify(_m)+`\\n`,"utf8");if(_lpEndBytes<0&&_m.type==="user"&&!_m.isCompactSummary&&!_m.isMeta){let _mc=_m.message?.content;let _txt=null;if(typeof _mc==="string"&&_mc.trim())_txt=_mc;else if(Array.isArray(_mc))for(let _c of _mc){if(_c.type==="text"&&_c.text?.trim()){_txt=_c.text;break}if(_c.type==="tool_result")break}if(_txt){_lp=_txt;_lpEndBytes=_byteOffset+_lineBytes}}_byteOffset+=_lineBytes;if(_lpEndBytes>=0&&_byteOffset>65536)break}let _titleToWrite="";if(_srcTitle)_titleToWrite=_srcTitle;else if(_lpEndBytes<0||_lpEndBytes>65536)_titleToWrite=_lp||"Forked conversation";if(_titleToWrite){if(_titleToWrite.length>200)_titleToWrite=_titleToWrite.slice(0,200);await Qn.promises.appendFile(c,JSON.stringify({type:"custom-title",customTitle:_titleToWrite,sessionId:o})+`\\n`)}}return this.loadedSessions.add(o),o}}asy', 'expected_count': 1}, {'old': 'anel_response"};case"fork_conversation":return{type:"fork_conversation_response",sessionId:await(await vi.load(this.cwd,this.logger)).forkSession(e.request.forkedFromSession,e.request.resumeSessionAt)};case"rewind_code":{let{userMessageId:r,d', 'new': 'anel_response"};case"fork_conversation":{let _m=await vi.load(this.cwd,this.logger),_src=e.request.forkedFromSession,_sid=await _m.forkSession(_src,e.request.resumeSessionAt);let _t="";try{let _lines=(await Qn.promises.readFile(zn.join(rl(_m.projectRoot),`${_src}.jsonl`),"utf8")).split(`\\n`),_c="",_a="";for(let _line of _lines){if(!_line)continue;try{let _M=JSON.parse(_line);if(_M.type==="custom-title"&&_M.customTitle)_c=_M.customTitle;if(_M.type==="ai-title"&&_M.aiTitle)_a=_M.aiTitle}catch(_){}}_t=_c||_a}catch(_){}if(!_t)_t="Forked conversation";this.onSessionStateChanged?.(_sid,"idle",_t,!0);return{type:"fork_conversation_response",sessionId:_sid}}case"rewind_code":{let{userMessageId:r,d', 'expected_count': 1}, {'old': 'as(s.id)):r}}async renameSession(e,t,r){return{type:"rename_session_response",skipped:await(await vi.load(this.cwd,this.logger)).renameSession(e,t,r)}}async messageRated(e){try{await this.w', 'new': 'as(s.id)):r}}async renameSession(e,t,r){let _r=await(await vi.load(this.cwd,this.logger)).renameSession(e,t,r);if(!_r)this.onSessionStateChanged?.(e,void 0,t);return{type:"rename_session_response",skipped:_r}}async messageRated(e){try{await this.w', 'expected_count': 1}, {'old': 'd?.(e.request.sessionId,e.request.state,e.request.title),{type:"update_session_state_response"}', 'new': 'd?.(e.request.sessionId,e.request.state,void 0),{type:"update_session_state_response"}', 'expected_count': 1}, {'old': 'geUpdate(t,r)}updateSessionState(e,t,r){this.sessionStates.set(e,{sessionId:e,state:t,title:r}),this.broadcastSessionStates()}setActivePanel(e){for(let[t,r]of this.s', 'new': 'geUpdate(t,r)}updateSessionState(e,t,r){let _p=this.sessionStates.get(e);this.sessionStates.set(e,{sessionId:e,state:t!=null?t:_p?.state??"idle",title:r!=null?r:_p?.title}),this.broadcastSessionStates();if(r!=null){let _pnl=this.sessionPanels.get(e);if(_pnl)_pnl.title=r}}setActivePanel(e){for(let[t,r]of this.s', 'expected_count': 1}, {'old': '=>this.broadcastUsageUpdate(),!!i,(u,d,p)=>{this.updateSessionState(u,d,p);for(let[f,m]of this.sessionPanels)if(m==', 'new': '=>this.broadcastUsageUpdate(),!!i,(u,d,p,_sk)=>{this.updateSessionState(u,d,p);if(!_sk){for(let[f,m]of this.sessionPanels)if(m==', 'expected_count': 1}, {'old': 'et(u,e),e.active)this.activeSessionId=u},(u)=>this.notifyLogin(u));this.allComms', 'new': 'et(u,e),e.active)this.activeSessionId=u}},(u)=>this.notifyLogin(u));this.allComms', 'expected_count': 1}, {'old': 'id 0,()=>this.broadcastUsageUpdate(),!1,void 0,(a)=>this.notifyLogin(a));this.allComms', 'new': 'id 0,()=>this.broadcastUsageUpdate(),!1,(H,D,O)=>{this.updateSessionState(H,D,O)},(a)=>this.notifyLogin(a));this.allComms', 'expected_count': 1}]), ('webview/index.js', [{'old': 't.id===t)return o}return}function x2(e){if(e.length>lit){let t=e.length-cit;return e.slice(t)}return e}function Dbe(e,t,i){let n=D6(t)', 'new': 't.id===t)return o}return}function x2(e){return e}function Dbe(e,t,i){let n=D6(t)', 'expected_count': 1}, {'old': 'ics:o,originalText:t}}let i=Fkt(t)||t,n=i.startsWith("/");return{type:"text",text:i,isSlashComman', 'new': 'ics:o,originalText:t}}let i=Fkt(t)||t,n=!1;return{type:"text",text:i,isSlashComman', 'expected_count': 1}, {'old': 'eturn null;if(t.isSynthetic)return null;return b(V8t,{session:e,message:t,index:i,conte', 'new': 'eturn null;if(t.isSynthetic)return null;let _ws=b(V8t,{session:e,message:t,index:i,conte', 'expected_count': 1}, {'old': 'setInputError:a,onCreateNewSession:l},i)}if(t.type==="assistant"){if(t.content.e', 'new': 'setInputError:a,onCreateNewSession:l},i);if(typeof t.uuid==="string"){let _r=t.uuid.startsWith("pfgk-broken-")?"broken":t.uuid.startsWith("pfgk-bookend-")?"bookend":t.uuid.startsWith("pfgk-seamClean-")?"seamClean":t.uuid.startsWith("pfgk-seam-")?"seam":t.uuid.startsWith("pfgk-bridge-")?"bridge":null;if(_r){let _h=(_ty,_pr,..._ch)=>b(_ty,{...(_pr||{}),children:_ch.length===0?void 0:_ch.length===1?_ch[0]:_ch});let _PFTOK={bookend:{accent:"#7ddcff",accentDim:"#246680",bg:"#142a35",panel:"#0c1c25",text:"#fff",textDim:"#b8d8e8",badge:"◆ RECONSTRUCTED · INFO",glyph:"◆",headline:"Conversation origin · reconstructed"},broken:{accent:"#ff6b6b",accentDim:"#8b2424",bg:"#3a1818",panel:"#2a1010",text:"#fff",textDim:"#e8b8b8",badge:"⛔ UNRECOVERABLE",glyph:"⛔",headline:"Incomplete transcript · reconstruction failed"},seam:{accent:"#ffc168",accentDim:"#8b6824",bg:"#3a2c14",panel:"#2a1f0c",text:"#fff",textDim:"#e8d4a8",badge:"⚠ IN-FILE REATTACH",glyph:"⚠",headline:"Compaction event · in-file orphan reattached"},bridge:{accent:"#ff9c5e",accentDim:"#8b4d24",bg:"#3a2418",panel:"#2a1810",text:"#fff",textDim:"#e8c4a8",badge:"↻ CROSS-FILE BRIDGE",glyph:"↻",headline:"Compaction origin · bridged from a sibling conversation"},seamClean:{accent:"#93a6c4",accentDim:"#3a4a64",bg:"#181d28",panel:"#0f141d",text:"#fff",textDim:"#c2cad8",badge:"◇ IN-FILE COMPACTION",glyph:"◇",headline:"Compaction event · crossed in-file"}};let _T=_PFTOK[_r];let _pd={};try{let _c=null;if(t.message&&typeof t.message.content==="string")_c=t.message.content;else if(typeof t.content==="string")_c=t.content;else if(Array.isArray(t.content)){for(let _bk of t.content){let _tx=_bk&&_bk.content&&_bk.content.text;if(typeof _tx==="string"&&_tx.slice(0,6)==="PFGK1:"){_c=_tx;break}}}if(typeof _c==="string"&&_c.slice(0,6)==="PFGK1:")_pd=JSON.parse(_c.slice(6))}catch(_){}let _kind=_pd.kind||_r;if(_kind==="seamClean"&&_PFTOK.seamClean)_T=_PFTOK.seamClean;let _badge=_pd.badge||_T.badge,_glyph=_pd.glyph||_T.glyph,_headline=_pd.headline||_T.headline,_rows=Array.isArray(_pd.rows)?_pd.rows:[],_body=_pd.body||"";function _pfDiagram(kind, T) {\n  // ---- SVG primitives ----\n  function L(x1, y1, x2, y2, c, w, da, op) {\n    return \'<line x1="\' + x1 + \'" y1="\' + y1 + \'" x2="\' + x2 + \'" y2="\' + y2 +\n      \'" stroke="\' + c + \'" stroke-width="\' + (w || 2) + \'"\' +\n      (da ? \' stroke-dasharray="\' + da + \'"\' : \'\') +\n      (op != null ? \' opacity="\' + op + \'"\' : \'\') + \'/>\';\n  }\n  function TX(x, y, str, c, o) {\n    o = o || {};\n    return \'<text x="\' + x + \'" y="\' + y + \'" text-anchor="\' + (o.a || \'start\') +\n      \'" style="font:\' + (o.f || \'500 9px monospace\') + \';fill:\' + c +\n      (o.ls ? \';letter-spacing:\' + o.ls : \'\') +\n      (o.it ? \';font-style:italic\' : \'\') +\n      (o.op != null ? \';opacity:\' + o.op : \'\') +\n      (o.tt ? \';text-transform:\' + o.tt : \'\') + \'">\' + str + \'</text>\';\n  }\n  // a node: optional accent ring (pivot), a solid or ghost dot, optional label above and sub below\n  function D(x, y, o) {\n    o = o || {};\n    var c = o.color, gr = o.gr || 6, xa = (gr * 0.5833).toFixed(2);\n    var g = \'<g transform="translate(\' + x + \',\' + y + \')"\' + (o.op != null ? \' opacity="\' + o.op + \'"\' : \'\') + \'>\';\n    if (o.ring) g += \'<circle r="14" fill="none" stroke="\' + c + \'" stroke-width="1" opacity="0.35"/>\';\n    g += \'<circle r="\' + gr + \'" fill="\' + (o.ghost ? \'none\' : c) + \'" stroke="\' + c + \'" stroke-width="2"\' +\n      (o.ghost ? \' stroke-dasharray="2 2"\' : \'\') + \'/>\';\n    if (o.ghost) g += \'<line x1="-\' + xa + \'" y1="-\' + xa + \'" x2="\' + xa + \'" y2="\' + xa + \'" stroke="\' + c + \'" stroke-width="1.3"/>\' +\n      \'<line x1="-\' + xa + \'" y1="\' + xa + \'" x2="\' + xa + \'" y2="-\' + xa + \'" stroke="\' + c + \'" stroke-width="1.3"/>\';\n    if (o.label) g += \'<text x="0" y="\' + (o.ldy || -17) + \'" text-anchor="middle" \' +\n      \'style="font:700 9px monospace;letter-spacing:0.16em;fill:\' + (o.lc || c) + \'">\' + o.label + \'</text>\';\n    if (o.sub) g += \'<text x="0" y="26" text-anchor="middle" style="font:500 9px monospace;fill:\' + (o.sc || c) + \';opacity:0.75">\' + o.sub + \'</text>\';\n    return g + \'</g>\';\n  }\n\n  var A = T.accent, X = T.text, Dm = T.textDim;\n  var H = \'700 9px monospace\', LS = \'0.16em\';\n  var Y = 74, G = 4, CL = 24, XL = 62; // chain baseline, line-to-node gap, continuation-stub length, left margin\n\n  // chain line between two nodes, ending the same gap G from each node\'s outer edge (so every node is centred between its lines)\n  function seg(ax, ar, bx, br, c, w, da, op) { return L(ax + ar + G, Y, bx - br - G, Y, c, w, da, op); }\n  function lead(fx, fr, c, w, da, op) { var x2 = fx - fr - G; return L(x2 - CL, Y, x2, Y, c, w, da, op); }   // short continuation stub, left\n  function trail(lx, lr, c, w, da, op) { var x1 = lx + lr + G; return L(x1, Y, x1 + CL, Y, c, w, da, op); }   // short continuation stub, right\n  // compact_boundary marker: rounded rect + glyph + label\n  function BND(cx) {\n    return \'<rect x="\' + (cx - 9) + \'" y="65" width="18" height="18" fill="none" stroke="\' + A + \'" stroke-width="1.5" rx="2"/>\' +\n      TX(cx, 78, \'⊜\', A, {a: \'middle\', f: \'700 11px monospace\'}) +\n      TX(cx, 100, \'compact_boundary\', Dm, {a: \'middle\', f: \'700 9px monospace\', ls: \'0.12em\'});\n  }\n\n  // seam / seamClean / bridge share this skeleton. arcHigh raises the recovery arc (and its caption) to clear the\n  // seam\'s centred PHANTOM label; the low arc rides lower, so its caption drops to keep the same 12px gap above the apex.\n  function cc(left, predSub, arc, mid, arcHigh) {\n    var u1 = 62, a1 = 128, P = 194, M = 256, B = 318, u2 = 396, a2 = 462;\n    var s = \'<svg viewBox="0 0 524 124" style="width:100%;height:124px;display:block">\';\n    s += TX(20, 15, left, Dm, {f: H, ls: LS}) + TX(322, 15, \'↳ POST-COMPACTION CHAIN\', A, {f: H, ls: LS});\n    // pre-compaction chain into the predecessor\n    s += lead(u1, 6, X) + D(u1, Y, {color: X, sub: \'user\', sc: Dm}) +\n      seg(u1, 6, a1, 6, X) + D(a1, Y, {color: X, sub: \'assistant\', sc: Dm}) +\n      seg(a1, 6, P, 14, X);\n    s += D(P, Y, {color: A, ring: 1, label: \'PRED\', sub: predSub, sc: Dm, ldy: -22}) + mid + BND(B);\n    // recovery arc PRED -> boundary; caption sits a fixed 12px above the apex on both arc heights\n    var cy = arcHigh ? 16 : 30;\n    s += \'<path d="M \' + P + \' 64 Q \' + M + \' \' + cy + \' \' + B + \' 64" stroke="\' + A + \'" stroke-width="2" fill="none" stroke-dasharray="5 4"/>\' +\n      TX(M, arcHigh ? 28 : 35, arc, A, {a: \'middle\', f: H, ls: \'0.14em\'});\n    // post-compaction chain\n    s += seg(B, 9, u2, 6, X) + D(u2, Y, {color: X, sub: \'user\', sc: Dm}) +\n      seg(u2, 6, a2, 6, X) + D(a2, Y, {color: X, sub: \'assistant\', sc: Dm}) +\n      trail(a2, 6, X);\n    return s + \'</svg>\';\n  }\n\n  // origin reached, chain whole: ROOT pivot + a mid-chain stitch point marking where a recovery reattachment sits\n  function bookendCard() {\n    var R = 62, u1 = 142, a1 = 222, ST = 302, u2 = 382, a2 = 462;\n    var s = \'<svg viewBox="0 0 524 124" style="width:100%;height:124px;display:block">\';\n    s += TX(20, 15, \'FULLY RECOVERED CHAIN\', A, {f: H, ls: LS});\n    s += D(R, Y, {color: A, ring: 1, label: \'ROOT\', sub: \'origin\', sc: Dm, lc: A});\n    s += seg(R, 14, u1, 6, X) + D(u1, Y, {color: X, sub: \'user\', sc: Dm}) +\n      seg(u1, 6, a1, 6, X) + D(a1, Y, {color: X, sub: \'assistant\', sc: Dm}) +\n      seg(a1, 6, ST, 11, X);\n    s += \'<g transform="translate(\' + ST + \',74)">\' +\n      \'<path d="M 0,-9 L 11,0 L 0,9 L -11,0 Z" fill="none" stroke="\' + A + \'" stroke-width="1.5" stroke-dasharray="3 2"/>\' +\n      \'<text x="0" y="3" text-anchor="middle" style="font:700 9px monospace;fill:\' + A + \'">↻</text></g>\' +\n      TX(ST, 100, \'stitch point\', Dm, {a: \'middle\', f: \'500 9px monospace\', op: 0.75});\n    s += seg(ST, 11, u2, 6, X) + D(u2, Y, {color: X, sub: \'user\', sc: Dm}) +\n      seg(u2, 6, a2, 6, X) + D(a2, Y, {color: X, sub: \'assistant\', sc: Dm}) +\n      trail(a2, 6, X);\n    return s + \'</svg>\';\n  }\n\n  // data loss: the lost run trails off the left edge through faint ghosts to a missing phantom, then a severed edge into the dead-end\n  function brokenCard() {\n    function sg(x) { // a small faint ghost = an older lost ancestor\n      return \'<g transform="translate(\' + x + \',74)" opacity="0.4">\' +\n        \'<circle r="5.6" fill="none" stroke="\' + A + \'" stroke-width="1.5" stroke-dasharray="2 2"/>\' +\n        \'<line x1="-3.3" y1="-3.3" x2="3.3" y2="3.3" stroke="\' + A + \'" stroke-width="1"/>\' +\n        \'<line x1="-3.3" y1="3.3" x2="3.3" y2="-3.3" stroke="\' + A + \'" stroke-width="1"/></g>\';\n    }\n    var g1 = 54, g2 = 82, g3 = 110, PH = 140, DV = 184, DE = 228, u1 = 306, a1 = 384, u2 = 462;\n    var s = \'<svg viewBox="0 0 524 124" style="width:100%;height:124px;display:block">\';\n    s += TX(20, 15, \'MISSING UPSTREAM\', Dm, {f: H, ls: LS}) + TX(300, 15, \'↳ VISIBLE TRANSCRIPT\', A, {f: H, ls: LS});\n    s += L(XL - 6 - G - CL, Y, PH - 8 - G, Y, A, 1, \'2 2\', 0.32); // lost run, dashed, off the left edge (stub fixed to the layout margin, not the ghosts)\n    s += sg(g1) + sg(g2) + sg(g3);\n    s += D(PH, Y, {color: A, ghost: 1, label: \'PHANTOM\', lc: A, gr: 8});\n    s += TX((g1 + PH) / 2, 100, \'unrecoverable\', Dm, {a: \'middle\', f: \'500 9px sans-serif\', it: 1, op: 0.85});\n    s += L(DV, 40, DV, 104, Dm, 1, \'3 4\', 0.55); // region boundary\n    s += seg(PH, 8, DE, 14, A, 1.5, \'4 4\', 0.85); // severed parent edge: phantom -> dead-end\n    s += D(DE, Y, {color: A, ring: 1, label: \'DEAD-END\', sub: \'missing parent\', sc: Dm});\n    s += seg(DE, 14, u1, 6, X) + D(u1, Y, {color: X, sub: \'user\', sc: Dm}) +\n      seg(u1, 6, a1, 6, X) + D(a1, Y, {color: X, sub: \'assistant\', sc: Dm}) +\n      seg(a1, 6, u2, 6, X) + D(u2, Y, {color: X, sub: \'user\', sc: Dm}) +\n      trail(u2, 6, X);\n    return s + \'</svg>\';\n  }\n\n  if (kind === \'bookend\') return bookendCard();\n  if (kind === \'broken\') return brokenCard();\n  if (kind === \'seam\') return cc(\'IN-FILE PRE-COMPACTION\', \'in-file\', \'in-file reattach\',\n    D(256, 74, {color: A, ghost: 1, label: \'PHANTOM\', lc: A, gr: 8}), true);\n  if (kind === \'seamClean\') return cc(\'IN-FILE PRE-COMPACTION\', \'in-file\', \'in-file link\', \'\', false);\n  if (kind === \'bridge\') return cc(\'CROSS-FILE PRE-COMPACTION\', \'sibling\', \'cross-file link\',\n    L(256, 40, 256, 104, Dm, 1, \'3 4\', 0.55), false);\n  return \'\';\n}let _allPfgk=[];try{let _am=e.messages.peek();for(let _m of _am)if(String(_m.uuid).startsWith("pfgk-"))_allPfgk.push(String(_m.uuid))}catch(_){}let _mi=_allPfgk.indexOf(String(t.uuid)),_tot=_allPfgk.length,_last=_mi===_tot-1;let _pad=function(_n){_n=String(_n);return _n.length<2?"0"+_n:_n};let _mono=\'"JetBrains Mono", ui-monospace, monospace\';_ws=_h("div",{className:"pfgkAlert pfgk-"+_r,"data-pfgk-role":_r,style:{background:_T.bg,border:"3px solid "+_T.accentDim,borderRadius:"4px",cursor:"pointer",color:_T.text,margin:"8px 0",fontFamily:"Inter, sans-serif"},title:"Click to jump to next Patch K marker",onClick:function(_e){var _all=Array.from(document.querySelectorAll("[data-pfgk-role]"));var _i=_all.indexOf(_e.currentTarget);if(_i<0)return;var _nx=_all[(_i+1)%_all.length];if(_nx)_nx.scrollIntoView({behavior:"smooth",block:"center"})}},_h("style",{key:"_s"},".pfgkAlert .content_xGDvVg.collapsed_xGDvVg{max-height:none!important}.pfgkAlert .truncationGradient_xGDvVg{display:none}.pfgkAlert .buttonContainer_xGDvVg{display:none}.pfgkAlert .actionButton_v2CdxQ{display:none}"),_h("div",{key:"_hd",style:{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"8px 16px",borderBottom:"1px dashed "+_T.accentDim,fontFamily:_mono,fontSize:"11px",fontWeight:700,letterSpacing:"0.14em",color:_T.accent,textTransform:"uppercase",gap:"16px"}},_h("span",{key:"_l",style:{display:"flex",alignItems:"center",gap:"10px"}},_h("span",{key:"_pk",style:{background:_T.accent,color:_T.bg,padding:"2px 6px",borderRadius:"2px",fontSize:"9px",letterSpacing:"0.18em"}},"PATCH K"),_h("span",{key:"_b"},_badge)),_tot>0?_h("span",{key:"_ct",style:{color:_T.textDim}},"Marker "+_pad(_mi+1)+" of "+_pad(_tot)):null,_h("span",{key:"_nx"},_last?"↺ cycle":"↓ next")),_h("div",{key:"_bd",style:{padding:"14px 18px 16px"}},_h("div",{key:"_hl",style:{display:"flex",alignItems:"center",gap:"12px",marginBottom:"12px"}},_h("span",{key:"_g",style:{fontSize:"22px"}},_glyph),_h("span",{key:"_h",style:{fontSize:"15px",fontWeight:700,color:_T.text}},_headline)),(function(){var _svg=_pfDiagram(_kind,_T);return _svg?_h("div",{key:"_dgp",style:{background:_T.panel,borderRadius:"3px",padding:"8px 12px",marginBottom:"12px",overflow:"hidden"},dangerouslySetInnerHTML:{__html:_svg}}):null})(),_h("div",{key:"_rp",style:{background:_T.panel,borderRadius:"3px",padding:"6px 16px 12px",fontSize:"12.5px",lineHeight:1.55}},_rows.map(function(_rw,_ri){return _h("div",{key:_ri,style:{display:"flex",alignItems:"baseline",gap:"12px",padding:"8px 0",borderBottom:_ri===_rows.length-1&&!_body?"none":"1px dotted "+_T.textDim+"40",fontFamily:_mono,fontSize:"11.5px"}},_h("span",{key:"_k",style:{color:_T.textDim,flex:"0 0 38%",textTransform:"uppercase",letterSpacing:"0.08em",fontSize:"10px",fontWeight:600}},String(_rw&&_rw[0])),_h("span",{key:"_v",style:{color:_T.text,flex:"1 1 auto",minWidth:0,wordBreak:"break-all"}},String(_rw&&_rw[1])))}),_body?_h("div",{key:"_by",style:{color:_T.textDim,marginTop:"12px",paddingTop:"12px",borderTop:"1px dotted "+_T.accentDim+"80",fontSize:"12.5px",fontFamily:"Inter, sans-serif"}},_body):null,_pd.tm?_h("div",{key:"_tm",style:{color:_T.textDim,opacity:0.8,fontSize:"11px",marginTop:"8px",fontFamily:_mono}},_pd.tm):null)))}}return _ws}if(t.type==="assistant"){if(t.content.e', 'expected_count': 1}]), ('webview/index.css', [{'old': ':var(--app-primary-background);position:sticky;z-index:2;background-image:linear-gradient(to bottom,var(--sticky-bg)calc(100% - 12px),transparent 100%),linear-gradient(to bottom,var(--app-secondary-background)calc(100% - 12px),transparent 100%);align-items:stretch;padding-top:14px;padding-bottom:12px;top:0}.message_07S1Yg.stickyHeader_07S1Yg:has([aria-expanded=true]){z-index:3}.fullEditor_07S1Yg .message_07S1Yg.stic', 'new': ':var(--app-primary-background);position:relative;z-index:auto;align-items:stretch;padding-top:14px;padding-bottom:12px}.message_07S1Yg.stickyHeader_07S1Yg:has([aria-expanded=true]){z-index:auto}.fullEditor_07S1Yg .message_07S1Yg.stic', 'expected_count': 1}])]


def main():
    args = sys.argv[1:]
    force = False
    if "--force" in args:
        force = True
        args = [a for a in args if a != "--force"]
    ext_dir = args[0] if args else find_default_ext_dir()
    if not ext_dir or not os.path.isdir(ext_dir):
        sys.exit(
            f"could not locate an installed {VERSION} extension; pass the "
            f"path explicitly or install it first"
        )

    # Decide state by checking the signature in extension.js. Recognize ANY
    # pfg-vX or pfg-vX.Y signature so a stale prior version (e.g. v1) is
    # detected as needing restore+reapply rather than silently no-op'd or
    # erroring on splice 0.
    ext_js = os.path.join(ext_dir, "extension.js")
    if not os.path.exists(ext_js):
        sys.exit(f"missing: {ext_js}")
    with open(ext_js, "r", encoding="utf-8", errors="surrogateescape") as f:
        head = f.read()
    has_current_sig = SIGNATURE in head
    sig_match = re.search(r'/\*pfg-v(\d+(?:\.\d+)?)\*/', head)
    other_sig = sig_match.group(1) if sig_match and not has_current_sig else None

    if has_current_sig and not force:
        print(f"Already patched (signature {SIGNATURE} present). Nothing to do.")
        return

    needs_restore = (force and has_current_sig) or other_sig is not None

    # Apply each file's splices
    for relpath, file_splices in SPLICES:
        target = os.path.join(ext_dir, relpath)
        bak = target + ".bak"
        if needs_restore:
            if not os.path.exists(bak):
                sys.exit(f"need to restore but no backup at {bak}")
            if other_sig:
                print(f"Stale patchset (file has v{other_sig}, current is v{PATCHSET_VERSION}); restoring {target} from {bak}")
            else:
                print(f"--force: restoring {target} from {bak}")
            shutil.copy2(bak, target)
        elif not os.path.exists(bak):
            shutil.copy2(target, bak)
            print(f"Backup -> {bak}")
        else:
            print(f"Backup exists: {bak}")

        with open(target, "r", encoding="utf-8", errors="surrogateescape") as f:
            s = f.read()
        for i, sp in enumerate(file_splices):
            old, new = sp["old"], sp["new"]
            expected = sp.get("expected_count", 1)
            cnt = s.count(old)
            if cnt == 0:
                # Maybe already patched; treat as applied if the result is present
                if s.count(new) >= max(1, expected):
                    print(f"  {relpath} splice {i}: already applied (skipped)")
                    continue
                sys.exit(
                    f"  {relpath} splice {i}: anchor not found "
                    f"(count=0). Bundle may have shifted; this "
                    f"prebuilt is for {VERSION}. Use the version-tolerant "
                    f"skill/apply-patch-fg.py instead."
                )
            if cnt != expected:
                sys.exit(
                    f"  {relpath} splice {i}: anchor count {cnt} != "
                    f"expected_count {expected}. Refusing to apply."
                )
            s = s.replace(old, new, expected)
            print(f"  {relpath} splice {i}: applied")
        with open(target, "w", encoding="utf-8", errors="surrogateescape") as f:
            f.write(s)

    # Syntax-check extension.js
    try:
        r = subprocess.run(
            ["node", "--check", ext_js], capture_output=True, text=True, timeout=30
        )
        if r.returncode == 0:
            print("node --check: OK")
        else:
            print("node --check FAILED:", r.stderr)
            sys.exit("Patched files may be broken; investigate before reload.")
    except FileNotFoundError:
        print("node not found on PATH, skipping syntax check.")

    print(f"All patches applied (prebuilt {VERSION}). Reload VSCode to activate.")


if __name__ == "__main__":
    main()
