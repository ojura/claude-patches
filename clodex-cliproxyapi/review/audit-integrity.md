# Subagent report: audit-integrity

- **Beat:** SHA256SUMS verification and coverage, external anchors (installed binary, CLIProxy source/binary, CCS, referenced tools), probe hygiene (network/filesystem/isolation/dependencies), fixture synthetic-ness, secret sweep, corpus durability.
- **Delivered:** failed on account session limit at ~13:18 UTC, self-resumed after the reset, delivered 2026-08-05 ~18:45 UTC.
- **Lead note:** during the outage the lead independently ran the residual checks (installed-binary hash match, CCS 8.8.1, leftover /tmp instrumented binaries, Go package declarations) — all corroborated by this report.

Verbatim report as delivered:

---

FINDINGS

1. Severity HIGH; Class PROVENANCE-GAP; Location ARCHITECTURE.md:29-30 (also probes/README.md:75-85). Claim: "CLIProxyAPI source used by the audits: /tmp/cliproxyapi-source" is an authoritative input. Observed: no commit hash, tag, or version for CLIProxyAPI anywhere in the corpus (grep of all .md/.py/.go for a88197f, commit/tag/version patterns: zero hits). The live dir is a grafted shallow clone at a88197f845c979132c8978ea223c6af05cc81536 = tag v7.2.116 (2026-08-03) — a durable pin exists upstream but was never recorded. Contrast: the Claude side gets TWO SHA-256s (binary d6e8882d…, extracted source e32e7ead…); the CLIProxy side gets only a /tmp path the corpus's own retention policy expects to vanish. Why it matters: once /tmp clears, the 4 Go regression probes ("copy each file into the matching package of a CLIProxyAPI source checkout") and every source-confirmed claim (stale-handshake authID/wsURL matching, watcher modify-vs-delete semantics, physical-GC gating) cannot be re-anchored to the audited revision. Compounding: websocket_late_frame.py's default CLIPROXY_BIN=/home/juraj/.ccs/cliproxy/bin/original/cli-proxy-api (exists, 62,470,888 bytes, mtime Aug 1) — that executed binary's version/hash is also unrecorded.

2. Severity MED; Class PORTABILITY; Location probes/claude/tombstone_removal.py:15 (also ARTIFACTS.md:53, probes/README.md:46-53). Claim: tombstone_removal.py is a retained reproduction whose only special requirement is CLAUDE_TOMBSTONE_PROBE_BINARY. Observed: base_copy() hard-reads /tmp/claude-rewind-successor-100.json, then shutil.copytree's the REWIND run's still-living tempdir (src/'config') and uses cwd=src/'work' — a triple dependency on prior-run /tmp state with no override. rewind_successor.py writes /tmp/claude-rewind-successor-result.json; the "-100" name exists only via an undocumented manual rename. KILL_DELAY_MS, PROBE_PORT, PROBE_SESSION, ONLY_INTERRUPT appear in no doc (grep all .md: zero). Why it matters: after /tmp wipe, the documented tombstone reproduction fails at line 15 and nothing tells a reproducer to first run rewind with KILL_DELAY_MS=100, rename its output, and keep its tempdir alive.

3. Severity MED; Class PROVENANCE-GAP; Location ARTIFACTS.md:49,58,59 (and reports/workflow12-local-state-checks.md:44-57). Claim: ARTIFACTS.md "answers where the evidence came from … and how to reproduce it"; retention keeps "localhost reproduction scripts". Observed: no retained probe generates: (a) evidence/claude/schedule-wakeup-token-counts.json (source /tmp/claude-ttl-count-results.json; classified "Executable-reproduced"; the count step — POSTing captured bodies to the configured localhost CLIProxy count endpoint yielding 1849/1777/1804 — has no script); (b) evidence/claude/manual-compact-requests.json (source /tmp/claude-interactive-compact-requests.json; physical_gc_forced_diagnostic.py writes …-forced-requests.json, a different file); (c) evidence/claude/semantic-auto-compact-control.json (source /tmp/claude-physical-gc-output.json; no retained probe emits it). Additionally the replacement-persistence crash-window sweep (report 1: turn omission at 0/10/25/50 ms vs reconstruction at 100/200/500 ms — a headline durability result) has NO probe and NO evidence file, prose only. Why it matters: several "Executable-reproduced/control" classifications are not reproducible from the corpus.

4. Severity MED; Class DRIFT; Location probes/README.md:42-53. Claim: "The instrumented binaries are deliberately not retained"; table lists /tmp historical defaults. Observed: all six listed defaults STILL EXIST in /tmp (claude-2.1.220-{ttl-mixed, force-context-hint, force-precompute-always, hint-precompute-stale, force-physical-gc-v2, remove-probe}) plus three unlisted siblings (force-precompute, force-physical-gc, enable-background-shortcut), and no hash of any instrumented binary is recorded anywhere. Why it matters: a rerun today silently picks up stale instruments whose semantic content is unverifiable, and a bun_handler.py recreation has no recorded target hash to verify against.

5. Severity LOW-MED; Class HYGIENE; Location probes/cliproxy/websocket_late_frame.py:4 vs probes/README.md:59. Claim: pexpect (physical_gc_forced_diagnostic.py) is the sole called-out non-dependency-free probe. Observed: websocket_late_frame.py imports third-party `websockets`; the word appears nowhere in any doc. Why it matters: the dependency inventory a runner relies on is incomplete.

6. Severity LOW; Class HYGIENE; Location probes/cliproxy/websocket_late_frame.py:34 (source: /tmp/cliproxyapi-source/internal/api/server.go:250). Claim: "Every retained probe is designed for localhost fixtures." Observed: the generated config sets `port: 18417` with no `host:` key; server.go builds Addr from cfg.Host:Port, so empty host → ":18417" → the launched cli-proxy-api listens on ALL interfaces for the run's duration (API key "local-test"). All outbound targets are loopback (base-url 127.0.0.1:47665, empty tempdir auth-dir), so no data can leave — but a LAN peer could drive the proxy mid-run. One-line fix: add `host: 127.0.0.1`.

7. Severity LOW-MED; Class PROVENANCE-GAP; Location fixtures/cliproxy-claude-models.json (vs probes/claude/clear_session_identity.py:15, reports/workflow12-local-state-checks.md:40). Claim: "Synthetic local Claude /v1/models response used by the retained probes." Observed: every `id` is "claude-fable-5-dd-" + a character-reversed real id (display_names left plain); literal "gpt-5.6-luna" occurs 0 times in the fixture, while the probe's own inline fallback catalog, the websocket probe config, and report 1 ("gpt-5.6-luna present" in the live GET /v1/models) all use plain ids. This is the current-model-prefix+reversal transform of agent-context model-id masking — the fixture bytes were captured through an id-rewriting layer. Mitigations verified: /tmp original is byte-identical and its mtime (2026-08-04 19:12) predates every run output, so the recorded evidence WAS generated against exactly this catalog (self-consistent), probes force model/limits via env so they function, and the transform is mechanically reversible (no leak). Why it matters: the retained "catalog" matches no real CLIProxy response and silently contradicts the corpus's plain-id representations of the same catalog; a reproducer diffing against a live proxy will see a spurious total mismatch.

8. Severity LOW; Class COVERAGE-GAP; Location ARCHITECTURE.md:31-32 (claims at 195, 350-365). Claim: "Installed CCS examined by the audits: 8.8.1". Observed: installed package.json is 8.8.1 (match), but no integrity hash of the installed tree exists anywhere in the corpus. The effort-route findings (normal CodexReasoningProxy preserves Claude-native effort; fallback suffix path overrides) are source-confirmed against this mutable local install — in an environment where installs are routinely locally patched (the Claude binary itself runs "(patched)"). Version alone cannot detect a modified CCS; it is an adequate pin only under an assumption the corpus never states.

9. Severity LOW; Class COVERAGE-GAP; Location evidence/claude/rewind-successor-{100,500,1000}ms.json + ARTIFACTS.md:55. Claim: three delay-variant controls plus a separately reported 0 ms omission. Observed: nothing INSIDE the result JSONs records the kill delay (no field; binding to 100/500/1000 ms is filename-only, assigned by hand), and the one variant that exhibits the actual defect (0 ms — resume omitted NEW_BRANCH) has no retained artifact; ARTIFACTS.md discloses "no distinct durable result file left to copy" and only report prose attests it. The three retained files ARE genuinely distinct runs (distinct roots 97bkj4nx/ss2wtppf/fnjbu8_j, distinct target UUIDs, distinct hashes — checked because sizes and mtimes were identical). Why it matters: the defect case rests on prose; the controls' delay labels are unverifiable from content.

10. Severity LOW; Class COVERAGE-GAP; Location probes/README.md:27-38. Claim: four listed probes "use the installed binary directly". Observed: schedule_wakeup_ttl.py:49-50 also hardcodes the installed binary for its 'five' and 'one' branches (only 'mixed' uses the instrumented default). An auditor enumerating which probes execute the installed binary from the README misses one.

CLEAN

- sha256sum -c SHA256SUMS from corpus root: 43/43 OK, 0 failed, 0 missing (full summary: every line "OK").
- Manifest coverage complete BOTH directions: sorted comm of on-disk files (44 minus SHA256SUMS itself) vs manifest entries is empty. ARCHITECTURE.md, ARTIFACTS.md, README.md, and probes/README.md ARE all hashed — the anticipated prose-doc coverage gap does NOT exist.
- Installed binary /home/juraj/.local/share/claude/versions/2.1.220 exists; sha256 computed now = d6e8882dce83be22a08456f6bdf8fa9b52c8bddf97fdcc1fc4d02209f0e5244e — exact match, zero drift since the corpus was written.
- 24/24 corpus artifacts whose declared /tmp originals still exist are byte-identical to them (all 18 evidence files, both reports, the fixture, plus the two "output.json" negatives) — ARTIFACTS.md's "byte copies of their original /tmp sources" claim fully verified while verification is still possible.
- Referenced relative tools all exist: ../util/bun_handler.py, ../util/patch_model_capabilities.py, ../util/test_patch_model_capabilities.py, ../docs/architecture.md; ~/claude-code exists (and ARCHITECTURE.md correctly demotes it to orientation-only).
- CCS install exists; version 8.8.1 matches the documented value.
- Network: every probe HTTP/WS server binds 127.0.0.1; every ANTHROPIC_BASE_URL is http://127.0.0.1:<port>; the ws stub upstream binds 127.0.0.1; no non-loopback dial target under default invocation anywhere (sole caveat = finding 6, a listen not a dial). Probe ports are all distinct (47656/47663/47664/47665/47667/47672/47673/47674/47675/47676/47678/18417).
- Filesystem: every run output goes to tempfile.mkdtemp dirs or explicit /tmp paths; zero writes into the corpus or $HOME from any probe; fixture read via parents[2] resolves to the corpus fixtures/ exactly as documented.
- Isolation: 10/10 Claude-launching probes set CLAUDE_CONFIG_DIR to a fresh tempdir AND scrub inherited CLAUDECODE/CLAUDE_CODE_SESSION_ID/CLAUDE_CODE_OAUTH_TOKEN/ANTHROPIC_AUTH_TOKEN/ANTHROPIC_CUSTOM_HEADERS before exec — better than the README promises.
- Env-var table (probes/README.md:46-53): all 7 variables match actual names and defaults exactly (CLAUDE_TTL_MIXED_BINARY, CLAUDE_CONTEXT_HINT_BINARY, CLAUDE_PRECOMPUTE_BINARY, CLAUDE_HINT_PRECOMPUTE_BINARY, CLAUDE_PHYSICAL_GC_BINARY, CLAUDE_TOMBSTONE_PROBE_BINARY, CLIPROXY_BIN).
- Go files: executor/*.go declare `package executor`, watcher file declares `package watcher` — match documented destinations internal/runtime/executor and internal/watcher.
- Secret sweep across all 44 files: sk-ant / sk-live / ghp_ / gho_ / eyJ-JWT / AKIA / xoxb / -----BEGIN → zero hits. Real endpoints (api.anthropic.com, openai.com, chatgpt.com, githubcopilot, generativelanguage) → zero hits anywhere, dial-able or prose. No email addresses. All tokens are test/sk-test/token-one/token-two/local-test/upstream-test; session UUIDs are the fixed synthetic ones; metadata.user_id device_ids are per-tempdir-generated hashes (distinct across runs within a single evidence file, account_uuid empty) — ephemeral, non-identifying.
- executor-suite.log matches its description exactly: 3 FAILs, precisely the three named device-profile/fingerprint tests; log contains no bearer/account/device/session values.
- Excluded-raw-log spot-checks PASS: the 1.2 MiB context-hint raw history's essential numbers survive in evidence/claude/context-hint-durability.json (per-request bytes/wrappers/cleared/raw counts) and report §Rung 4/5 (402,116 → 253,221 → 402,241 bytes); the executor suite is retained in full plus summarized (6/6 focused reproduced, 3 upstream env FAILs); tombstone byte numbers (53,069,996→53,071,918; 29,273→9,020) are carried in the report.

GAPS

- Extracted-source hash e32e7ead… (ARCHITECTURE.md:26) unverifiable without running bun_handler.py extraction (execution prohibited by my brief). [Lead note: verified independently — /tmp/claude-2.1.220.js matches e32e7ead…/21,636,716 exactly.]
- Whether the surviving /tmp instrumented binaries semantically match their described forcings — unverifiable statically; no recorded hashes (finding 4).
- The 1849/1777/1804 count totals depended on the live configured CCS cliproxy instance; "No external generation or remote service was contacted" is the reports' own assertion — nothing in the corpus lets me independently confirm the count endpoint stayed local.
- CCS installed-tree integrity: nothing to check against (finding 8).
- The 0 ms rewind result: no artifact exists to audit (finding 9; disclosed by ARTIFACTS.md).
- Report-cited per-run request captures beyond ARTIFACTS' mapping (e.g. /tmp/claude-ttl-{five,one,mixed}-requests.json) were intentionally excluded by policy; I did not audit their survival.
