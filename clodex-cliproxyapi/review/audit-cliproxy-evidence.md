# Subagent report: audit-cliproxy-evidence

- **Beat:** CLIProxy-side evidence artifacts, Go/Python probe quality (spurious-pass risk), static verification against /tmp/cliproxyapi-source (a88197f8 = v7.2.116).
- **Delivered:** completed ~13:18 UTC; the original send did not reach the lead ("main" not addressable from a teammate); identical report redelivered on request 2026-08-05 ~18:45 UTC.
- **Lead verification:** HIGH 1 (version split, tag identity), the upstream sharing-test names (`:113`, `:234`), and the cloak transform (`models.go:9`) re-verified by the lead (confirmed).

Verbatim report as delivered:

---

FINDINGS

1. Severity HIGH; Class PROVENANCE-GAP; Location ARCHITECTURE.md:29-30 (also ARTIFACTS.md:110-121, probes/cliproxy/websocket_late_frame.py:5). Claim: "CLIProxyAPI source used by the audits: `/tmp/cliproxyapi-source`". Observed: the corpus pins NO CLIProxy commit and NO CLIProxy binary hash anywhere, while the same "Authoritative inputs" section records the Claude binary SHA-256, extracted-source SHA-256, and byte size. Worse, the two CLIProxy evidence lanes ran against DIFFERENT versions: the Go probes compiled against /tmp/cliproxyapi-source (reflog: single clone 2026-08-04, HEAD a88197f8, dated 2026-08-03), but websocket_late_frame.py's default CLIPROXY_BIN=/home/juraj/.ccs/cliproxy/bin/original/cli-proxy-api reports "Version: 7.2.112, Commit: a63da8ae, BuiltAt: 2026-07-31". Neither identity, nor the fact of the split, is recorded. Every CLIProxy "executable-reproduced" claim is version-relative, and the corpus's own authority discipline demands exactly the pinning it omits.

2. Severity MED; Class PROVENANCE-GAP; Location ARCHITECTURE.md:449-462 ↔ evidence/cliproxy/executor-suite.log ↔ reports/workflow12-local-state-checks.md:28. Claim: "Local Go checks reproduced all of the following" (seven "Executable-reproduced replay defects"). Observed: no retained artifact contains a per-test run record for the six replay/count tests. executor-suite.log is a NON-VERBOSE package run: its only named tests are the three device-profile FAILs (`X-Stainless-Os = "MacOS", want "Linux"`); passing tests print nothing, so the log can neither name the six probe tests nor prove the probe file was even compiled into that 10:56:40 run. The two dedicated verbose logs cover only the credential (12:56:37) and partial-write (12:58:06) tests. The sole run evidence for defects 1-6 is one prose sentence, "Focused tests: 6/6 passed". Uniquely among executable sections, ARCHITECTURE.md:449-462 carries no `Capture:` link — inconsistent with the corpus's own standard.

3. Severity MED; Class UNSUPPORTED; Location ARCHITECTURE.md:461-462 (item 7 under "Local Go checks reproduced all of the following"). Claim: "Existing tests deliberately permit sharing across downstream client keys and Codex auth entries for the same supplied session." Observed: this is a claim about UPSTREAM CLIProxy's own tests, not the retained suite — the retained probe file's six functions map to items 1-6 plus the header-precedence test only, and both reports enumerate only six checks. The claim is TRUE at a88197f8: TestCodexExecutorReasoningReplayCacheSharesSameSessionAcrossClientKeys (codex_executor_reasoning_replay_cache_test.go:113; fatal message "replay scope should ignore client API key for the same session") and ...SharesSameSessionAcrossCodexAuths (:234; "want same-session replay across auths") assert sharing as desired behavior. But the corpus never names these tests, retains neither them nor any log line showing them, and files the observation under a heading attributing it to local reproduction. Honest labeling: cite the upstream test names, class as source-confirmed-at-unpinned-commit.

4. Severity MED; Class OVERSTATED; Location ARCHITECTURE.md:189 and :993-1004 ↔ evidence/cliproxy/credential-refresh-stale-handshake.log. Claim: "stale reuse is executable-reproduced" via "A focused watcher-supported auth-file fixture resolved the question: 1)…2)…3)…". Observed: the numbered narrative stitches TWO isolated unit tests in different packages (watcher: file rewrite → AuthUpdateActionModify same ID; executor: same ID/URL + token-two header → `connTwo != connOne` fails, i.e., same-instance reuse) plus two source-only glue links (modify path never closes sockets — service_auth.go:110-128 registers models only, while only the Delete branch at :334-352 calls CloseCodexWebsocketSessionsForAuthID; and the serving path re-offers the same authID/URL). No end-to-end flow (file rewrite → watcher event → next request reuses stale socket) was ever executed. Each part is solid — I verified both glue links in source — but promoting the composed chain to "executable-reproduced" is precisely the class promotion ARCHITECTURE.md:177-179 forbids; report line 121 honestly says "two-part executable control" while ARCHITECTURE presents it as one fixture. Also: the watcher probe invokes w.addOrUpdateClient() directly rather than the fsnotify event (mitigated: events.go:109/:120-127 routes Write/Create and even atomic-replace Rename to the same addOrUpdateClientLocked).

5. Severity LOW; Class PROBE-DEFECT; Location probes/cliproxy/executor/replay_and_count_regressions_test.go:49-53. Claim (ARCH item 3): changed instructions/tools/effort produce the same replay fingerprint. Observed: the test asserts only `gotA != gotB → Fatal` with no non-degeneracy guard (its sibling at :61 checks `gotA == ""`). If the `input` path were absent both sides degrade to the empty-prefix digest and still "pass". More substantively, codexReplayInputPrefixFingerprint(items, end) (codex_executor_reasoning.go:453-464) by signature never sees top-level fields, so the probe passes tautologically and would keep "reproducing the defect" even after a fix adding a semantic digest at a different layer — the inverted test cannot detect that class of fix.

6. Severity LOW; Class PROVENANCE-GAP; Location evidence/cliproxy/credential-refresh-inconclusive-control.json ↔ ARTIFACTS.md:69 ↔ ARCHITECTURE.md:988-991. Claim: "the earlier configured-key edit was inconclusive because that loader did not expose the replacement token." Observed: the artifact is consistent with that reading — three distinct connections (cids 1,2,3, i.e., fresh handshakes, not retained-socket reuse) all carry `"authorization": "Bearer token-one"`, showing even new dials never saw token-two — but it contains no record that a token-two edit occurred, when it occurred relative to cids 2/3, or which config was edited, and its generating probe (/tmp root `cliproxy-ws-refresh-cyjwttzr`) was not retained. The decisive premise of the "superseded control" interpretation rests entirely on report narrative.

7. Severity LOW; Class HYGIENE; Location probes/cliproxy/websocket_late_frame.py:5. Observed: hardcoded non-portable default BIN='/home/juraj/.ccs/cliproxy/bin/original/cli-proxy-api' (user-specific; currently resolves to 7.2.112/a63da8ae — see Finding 1) and fixed ports 47665/18417; override is documented in probes/README.md, but the default silently selects an unversioned binary, and the probe's proxy-side log (/tmp/cliproxy-ws-probe.log), cited by report line 117, was not retained — only the result JSON was.

CLEAN

- credential-refresh-stale-handshake.log ↔ claims: log shows exactly one connect + one session_closed disconnect for auth=stable-auth-id (no second handshake); executor probe asserts first Authorization == "Bearer token-one" (:48-50), offers token-two on same ID/URL (:57), same-instance identity `connTwo != connOne → Fatal` (:61) — not a nil-check — plus a 200 ms no-second-handshake wait (:64-68). Watcher probe asserts Modify action, second.ID == first.ID, access_token == token-two (:56-64). Not spuriously passable.
- partial-write-full-resend.log ↔ ~1013-1031/D027: byte counts appear literally in the probe (io.ReadFull into 64*1024 buffer, "want 65536" :90-91; strings.Repeat("X", 32*1024*1024) :72; `got < len(largeInput) → Fatal` :97-99; connections == 2 :104-106); resend is automatic (single Execute() call, nil error :85-87); log shows send_error "connection reset by peer" then a fresh connect. Caveat: the numbers live in silent-on-pass assertions plus the PASS line, not in the log text; the "exactly 65,536" is fixture-imposed (ReadFull of a 64 KiB buffer over a 32 MiB stream cannot return otherwise), which the doc presents honestly as server behavior.
- Source at a88197f8 confirms the resend mechanics: invalidateUpstreamConn(sess, conn, "send_error", …) → shouldRetryCodexWebsocketSend (retries any non-request-scoped error, codex_websockets_connection.go:70-76) → fresh ensureUpstreamConn → full-body rewrite (codex_websockets_execute.go:206-236).
- websocket-late-frame-crossover.json ↔ ~972-985: b_events terminal is resp_A_LATE, extra_events [], up_request_count 2; probe construction guarantees the crossover frame is only emitted on the retained connection (idx==2 in the single upstream handler), so B-on-retained-socket is proven, and the stub demonstrably sent the real resp_B 0.15 s later (probe line 19) which never reached downstream — "undeliverable" is observed, not inferred.
- ~999-1001 source-confirmed: retained match compares only trimmed authID + wsURL (codex_websockets_session.go:337); ensureUpstreamConn returns sess.conn without consulting headers (:475-489); only auth DELETION closes Codex sockets (service_auth.go:334-352 vs :110-128).
- ~962-967 source-confirmed: ConvertClaudeRequestToCodex builds {"model":"","instructions":"","input":[]} fresh from the full messages array; codex_executor_execute.go:58 and codex_executor_stream.go:57 both sjson.DeleteBytes(body, "previous_response_id"); no response-ID continuation store exists in the codex HTTP/SSE path (only deletes + an error-string matcher in codex_executor_terminal.go:333).
- ~466-481 source-confirmed: codexReplayInputPrefixFingerprint hashes only inputItems[i].Raw with an item separator — raw member order/escaping feed the digest; no instructions/tools/betas/effort/output-mode anywhere in it.
- Replay probe ↔ source: unanchored marked turns bypass the fingerprint entirely (len(callIDs)==0 && assistantFingerprint=="" → return true, codex_executor_reasoning.go:358-360); distinct encrypted_content survives the dedup filter; assistant fingerprint concatenates part texts with no separator → ['ab','c']/['a','bc'] genuinely collide (test guards non-empty); SanitizeClaudeToolID regex [^a-zA-Z0-9_-]→_ makes call:a/call/a → call_a, and codexReplayComparableCallIDs can never emit "" (nil for empty, generator fallback otherwise); countCodexInputTokens only walks input when IsArray() → string input counts 0 exactly as asserted.
- A6 RESOLVED (not unsupported): the ~547-549 "focused CLIProxy test" IS retained — TestWorkflowProbeClaudeHeaderOverridesBodySession (replay_and_count_regressions_test.go:81-94); source confirms header-over-payload precedence (ExtractClaudeCodeSessionID: header first, claude_code_session.go:23-27; scope format claude:<sid>:agent:<agent>).
- ~1103-1121 ↔ fixture: Claude model formatting emits exactly id/object/owned_by/type/display_name/max_input_tokens/max_tokens/created_at with synthetic defaults 200000/64000 on missing values (model_registry.go:22-23, 1204-1229; server_routes.go:654-680); registry is last-writer-wins for the aggregate Info (`existing.Info = cloneModelInfo(model)`, model_registry.go:499); default cloaking = "claude-fable-5-dd-" + reversed ID unless DisableCloakingModelList (client/claude/models/models.go:50-55). fixtures/cliproxy-claude-models.json matches the handler shape field-for-field, 14 entries, IDs correctly cloak-reversed (…-anul-6.5-tpg ↔ gpt-5.6-luna), wrapper data/first_id/has_more/last_id matches BuildResponse.
- Probe portability: all four Go files declare the correct packages (executor, watcher) matching probes/README.md destinations; every unexported symbol referenced exists at a88197f8 (ensureUpstreamConn, NewCodexWebsocketsExecutor, codexWebsocketSessionStore, getOrCreateSession, CloseExecutionSession, ExecutionSessionMetadataKey, DisableImageGenerationAll, insertCodexReasoningReplayTurns, CodexReasoningReplayTurnType, all four fingerprint/ID/count functions, validCodexReasoningEncryptedContentForTestSeed [upstream test helper :24], NewWatcher/SetConfig/SetAuthUpdateQueue/addOrUpdateClient/AuthUpdate actions).
- websocket_late_frame.py is loopback-only (127.0.0.1 upstream + proxy), drives a real binary + fake upstream, writes only under /tmp; its config keys (codex-api-key, base-url, websockets: true) and the --local-model flag all exist at a88197f8 (config_types.go:401-421; cmd/server/main.go:111).
- ARTIFACTS.md log mapping is accurate as far as it goes: row 70 correctly describes the credential log as holding both tests; row 71 the partial-write test; row 72 correctly characterizes executor-suite.log ("full executor-package run; three device-profile expectations fail" — matches the three FAILs verbatim). The gap is what it omits (Finding 2), not what it states.
- SHA256SUMS: all 43 entries verify OK; coverage complete (every corpus file except SHA256SUMS itself is hashed).

GAPS

- READ-ONLY mandate: I executed nothing; all PASS/FAIL claims are verified statically (assertion logic + retained logs), not re-run.
- Whether the six replay/count probe tests were compiled into the 10:56:40 executor-suite run is inherently indeterminable from the non-verbose log (they emit no log lines and passed silently if present) — the core of Finding 2.
- Version skew: all my source confirmations are against a88197f8 (cloned 2026-08-04, never moved per reflog — so it plausibly IS the tree the 2026-08-05 Go probes compiled against, but the corpus itself never says so). The python late-frame result was produced by an unrecorded binary — if run with the documented default, that binary is 7.2.112/a63da8ae (2026-07-31), and none of my a88197f8 checks formally cover it; I found no reason to expect the retained-socket/terminal-frame routing differs between the two, but that is inference, not verification.
- The inconclusive-control's generating probe and the late-frame run's proxy-side log were not retained, so those two artifacts cannot be independently re-derived from the corpus alone.
- I did not audit evidence/claude/, probes/claude/, or the Claude-binary claims — other reviewers' beats.
