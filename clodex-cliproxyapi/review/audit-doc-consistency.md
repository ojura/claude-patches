# Subagent report: audit-doc-consistency

- **Beat:** internal consistency of the documentation set — superseded-claims discipline, decision register vs body, evidence-class labels, rung renumbering, link/index integrity, notation coherence.
- **Delivered:** 2026-08-05 ~13:09 UTC.
- **Lead verification:** findings 1–3 re-verified against files by the lead (confirmed).

Verbatim report as delivered:

---

FINDINGS

1. Severity MED; Class OVERSTATED. Location: ARCHITECTURE.md:451-462 vs reports/workflow12-local-state-checks.md:20-28, ARTIFACTS.md:40 and :105, probes/cliproxy/executor/replay_and_count_regressions_test.go. Claim: ARCH:451 "Local Go checks reproduced all of the following:" with item 7 (:461-462) "Existing tests deliberately permit sharing across downstream client keys and Codex auth entries for the same supplied session" — vs report 1:28 "Focused tests: 6/6 passed", ARTIFACTS:40 "six replay/count defects", ARTIFACTS:105 listing six behaviors with no sharing item; the retained test file has exactly 6 test functions, none about cross-caller sharing, and executor-suite.log names no such test. An item under an "Executable-reproduced replay defects" header has no executable witness anywhere in the retained corpus — its real basis is reading upstream test intent, which is the source→executable promotion the doc's own rule (ARCH:176-179) forbids.

2. Severity MED; Class INCONSISTENT. Location: ARCHITECTURE.md:896-899 vs ARCHITECTURE.md:1415-1425 and :1427-1441. Claim: ":896 Source and exact-function analysis also establish a conditional stale dynamic-tool case" (correctly source-level, never upgraded) — but it appears in neither "Still unexercised or runtime-incomplete" (:1417) nor "No longer open as broad discovery questions" (:1427), while its siblings "dynamic ScheduleWakeup description drift" (:1433) and "same-UUID and cross-model stale precompute" (:1435) are listed closed. A reader scanning the closure ledgers will conclude every precompute-staleness case is closed although this one was never executed — exactly the false-closure the remaining-work list exists to prevent.

3. Severity MED; Class BROKEN-LINK. Location: probes/README.md:43-44 vs ARCHITECTURE.md:28 and ARTIFACTS.md:119. Claim: probes/README ":43 Recreate them ... through `../util/bun_handler.py`" — from probes/, that resolves to clodex-cliproxyapi/util/bun_handler.py, which does not exist (verified); every other `..` in the same file (`../reports/`, `../ARTIFACTS.md`, `../SHA256SUMS`) resolves to project root, while ARCH/ARTIFACTS use the identical string correctly from project root (claude-patches/util/bun_handler.py exists). The one instruction a reproducer of the six instrumented binaries must follow points at a nonexistent path from the file that gives it.

4. Severity LOW; Class INCONSISTENT. Location: probes/README.md:18-24 vs probes/claude/*.py (all ten). Claim: "Claude probes read: ../fixtures/cliproxy-claude-models.json relative to the project root" — relative to the project root `../fixtures` is claude-patches/fixtures (nonexistent); the scripts actually resolve `Path(__file__).parents[2]/'fixtures'` = <project-root>/fixtures. The path prefix and its qualifier contradict each other; the correct statement is "fixtures/ under the project root".

5. Severity LOW; Class INCONSISTENT. Location: reports/workflow12-outstanding5-local-checks.md:163 vs ARTIFACTS.md:55. Claim: report ":163 Captures: ... and /tmp/claude-rewind-successor-result.json for the 0ms case" vs ARTIFACTS ":55 the separately reported 0-ms omission had no distinct durable result file left to copy". The report cites a capture the index says did not survive; only reading rewind_successor.py:52 (fixed output path, overwritten each run) reconciles them — net effect: the rewind narrowing retains only its negative controls, with no retained capture of the 0-ms defect half.

6. Severity LOW; Class INDEX-GAP. Location: reports/workflow12-outstanding5-local-checks.md:117 (also :40-42, :65, :87) vs ARTIFACTS.md:21-30. Claim: report cites "/tmp/cliproxy-ws-probe.log" (and /tmp/claude-ttl-{five,one,mixed}-requests.json, hint-precompute-stale-requests, precompute-cross-model-requests) — ARTIFACTS neither retains these nor names them among exclusions, though its selection policy retains "compact logs that show an exact transport transition" and it did retain the two analogous focused transport logs (:70-71) and names two other excluded raw captures with sizes (:27-29). A reader cannot distinguish deliberate exclusion from loss for these five cited captures.

7. Severity LOW; Class INCONSISTENT. Location: ARCHITECTURE.md:190 and ARTIFACTS.md:71 vs ARCHITECTURE.md:1013-1015 and reports/workflow12-outstanding5-local-checks.md:138. Claim: summaries say "failed after 65,536 bytes left the process" / "After a 65,536-byte partial transmission" (client-side transmitted count) while the primary statements say the stub "read exactly 65,536 bytes from the first frame" (server-side read count; client may have written more). The corpus's two summary rows quantify a measurement the fixture did not take.

8. Severity LOW; Class STALE-CLAIM (discipline). Location: reports/workflow12-local-state-checks.md:1-3 and reports/workflow12-outstanding5-local-checks.md:1-5; ARTIFACTS.md:40-41; concrete instance report 1:83 vs report 2:89-101 and ARCHITECTURE.md:887-894. Claim: neither report carries any banner/frontmatter tying it to the corrected conclusions (title + "Date: 2026-08-05" only) and the ARTIFACTS report rows carry no staleness annotation; report 1's ":83 The same-UUID stale-precompute scenario was not reproduced in this pass" is protected only by its own "in this pass" scoping, with no forward pointer to the later executable reproduction. The superseded table (:183-184) defends ARCHITECTURE.md readers, not report readers.

9. Severity LOW; Class INCONSISTENT. Location: ARCHITECTURE.md:970-984, :843-872, :874-899, :564-599 vs :559, :739-742, :759, :1010, :1031. Claim: five executable findings get inline "Capture:" evidence links (/clear, tombstones, fork, credential, partial-write) while the equally executable late-frame crossover, context-hint durability, cross-model/same-UUID precompute, and TTL-variant findings cite none, though their files exist and are indexed (ARTIFACTS:48-52, :68). An auditor tracing "executable-reproduced" claims from ARCHITECTURE alone gets direct pointers for only half of them.

CLEAN

- Superseded rows 1-11: full-corpus grep found no artifact asserting any superseded claim as current; every historical phrasing ("inconclusive", "source-only", "unexercised", "design concern", raw-body "restoration") occurs only in the table itself, in explicit "This supersedes…" sentences (ARCH:731, :751, :1003, :1024), or in already-resolved narrative (report 2:121→124).
- ARTIFACTS flags the two artifacts that genuinely conflict with corrected conclusions: credential-refresh control marked "Superseded control" (:69); rewind 0-ms absence explained (:55).
- D001–D033 all have supporting body text; every annotated status matches the body's evidence class (D026-D029 executable ↔ ARCH:719-742/:744-759/:987-1010/:1012-1031; D030/D032 source-confirmed ↔ :761-774/:806-814; D031/D033 runtime-pending ↔ :782-804/:816-830 and the remaining-work list). No decision promotes source-confirmed to executable.
- Evidence-class labels agree across ARCHITECTURE, ARTIFACTS, and both reports for all twelve facts checked (tombstone ×2, credential-refresh, partial-write, late-frame, fork ownership, hint non-durability, cross-model + same-UUID precompute all executable; background barrier, GC child pruning, CCR hydration source-confirmed/runtime-pending; admission reserve executable). The source-level dynamic-tool stale case is never upgraded anywhere (report 2:16 claims reproduction only for materialization, cross-model, same-UUID).
- Rung renumbering: report 1 contains zero rung references; report 2 uses new numbering exclusively, opens with the full "Corrected patch ladder status" table (:9-17), and every heading pairs number with topic ("Rung 5 — precomputed compaction integrity"), so no old/new misread is available; no old-numbered usage survives anywhere outside ARCH's mapping table (:312-322).
- All relative markdown links in all six .md files resolve; `../util/{bun_handler,patch_model_capabilities,test_patch_model_capabilities}.py` and `../docs/architecture.md` exist from project root.
- SHA256SUMS covers exactly the 43 non-SUMS files (set-diff empty); ARTIFACTS indexes all 16 claude + 5 cliproxy evidence files, 1 fixture, 10 + 5 probes, 2 reports; no ARTIFACTS entry references a missing local file; README's layout tree matches the actual tree.
- probes/README env-var table matches the scripts verbatim: all six variables and all six historical defaults (incl. CLAUDE_TTL_MIXED_BINARY → /tmp/claude-2.1.220-ttl-mixed at schedule_wakeup_ttl.py:51); the four installed-binary probes default to /home/juraj/.local/share/claude/versions/2.1.220; CLIPROXY_BIN matches.
- Numeric cross-checks all pass, prose↔prose and prose↔evidence-file: 402,116/253,221/402,241 (hint JSON exact); 53,069,996/53,071,918 + rc=0 + target_remains; 29,273/9,020 + truncated_before_kill; 2,933/2,685/2,734 + all three SHA-256s; 1,849/1,777/1,804; 2,276-byte wrapper; 0–50 ms absent vs 100+ reconstructed; 5/20 MiB; 64 KiB; 100 ms batching; margin arithmetic coheres (372−20=352, −3→349, −13→339; 372−13=359, −3→369); tombstone-removal.json's extra 29,289→30,995 record matches ARTIFACTS' "non-truncating control".
- Auto-compaction transition coherent everywhere: origin scoped "at the time: disabled" (:208); ":343 currently enabled … disabling it is no longer the current operating state"; report 1's `autoCompactEnabled: true` is fixture configuration; no other operating-state assertion exists in the corpus.
- Plane/dimension letter reuse: outside the two definition tables every bare bold symbol is dimension-unique C/U/E except "**M** … **R/S**" at :1230-1231, where plane-only **M** and the plane-example idiom (:133-139) disambiguate — no genuinely ambiguous bare-symbol sentence found.
- No uncontrolled report↔ARCHITECTURE contradiction: report 1's crash-window paragraph already states the corrected turn-omission conclusion (:57); every report 2 conclusion matches ARCH row-for-row (128k/32k, rewind narrowing, GC source-resolution, background/CCR source-only status, the three macOS-profile suite failures).
- Fixture sanity: 14 synthetic models carrying display_name/max_input_tokens/max_tokens — consistent with the corrected non-IDs-only /v1/models claim and coincidentally with report 1's live "14 models".

GAPS

- The 11 "earlier claims" in the superseded table have no in-corpus source documents (the pre-correction reports were excluded from retention), so the original wording and provenance of what was superseded cannot be verified; the table is its own only witness.
- ARTIFACTS' "Original source" columns point to /tmp paths; the byte-copy and probe-normalization claims (:113-116) are unverifiable now — only local-copy hashes exist. (Some /tmp material still exists today — cliproxyapi-source, cliproxy-runtime-probe — but the result JSONs were not re-verifiable against origin.)
- On finding 1: /tmp/cliproxyapi-source does contain upstream replay-cache tests using a shared execution session, so ARCH item 7 is plausibly TRUE against upstream source — the defect is that the retained corpus contains no witness and the reports never state it; truth itself is outside the corpus.
- Minified-symbol claims (vSe, Fny, Oe, eFe, qLo, uZc, Yxd, Efo, lst, Hn, dGr…) and all binary-internal behavior are unverifiable from the document set; this review verified consistency of statements about them, not the statements.
- Note: the tasking said "evidence/ 13+5"; the actual tree and ARTIFACTS both have 16+5 — verification was done against the actual tree.
