# Subagent report: audit-claude-evidence

- **Beat:** Claude-side "executable-reproduced" claims vs their captures in evidence/claude/ and reports/; probe spot-checks.
- **Delivered:** 2026-08-05 ~13:10 UTC.
- **Lead verification:** HIGH 1, HIGH 2, MED 4, MED 7, MED 9 re-verified against files by the lead (all confirmed; the MED 9 fixture scheme was later source-confirmed as CLIProxy's cloak transform by the cliproxy beat).

Verbatim report as delivered:

---

FINDINGS

1. Severity HIGH; Class UNSUPPORTED; ARCHITECTURE.md:234-248. Claim: "A fresh Luna session, safe mode, no persistence, identical input, and loopback stub proved the admission equation independently of source inspection … output 10,062 passed and 10,070 blocked, with a predicted boundary of 10,064." Observed: the numbers 10,062/10,070/10,064, the 32k→~20k boundary shift, and the 128-token case appear NOWHERE else in the corpus — no capture, no probe, no report (grep over all 43 files hits only ARCHITECTURE.md itself; workflow12-outstanding5-local-checks.md:12 merely points back at "the earlier binary-search proof" as "the executable control" without carrying it). The corpus's headline "Executable proof of the reserve equation" — the root-cause claim of the whole 336k/372k investigation — fails the retention policy's own premise (README.md:42: excluded raw logs were "already summarized by a focused result and report"); this one is summarized by neither.

2. Severity HIGH; Class UNSUPPORTED; ARCHITECTURE.md:603-613. Claim: "With a loopback `ANTHROPIC_BASE_URL`, the authoritative bundle produced: Hn = firstParty, Dc = true, rm = true, dj = true, dGr = false, Yd = false." Observed: no capture, probe, or report mentions Hn/Dc/rm/dj/dGr/Yd or any custom-base classification run (grep: only ARCHITECTURE.md); the sole tangential corroboration is `"provider":"firstParty"` in modelUsage telemetry inside clear-session-identity.json. An executable-framed claim ("produced") that feeds the isDirectAnthropicAPI/isGenericCustomBase design predicate (625-626) is unreproducible from the corpus.

3. Severity MED; Class UNSUPPORTED; ARCHITECTURE.md:643-659. Claim: "A 1.9 MiB Bash result was reduced to a 2,276-byte persisted-output wrapper" + the 0/10/25/50/100/200/500 ms crash table. Observed: backed only by prose in reports/workflow12-local-state-checks.md:44-57, which — uniquely among that report's executable sections — cites no capture path; no evidence/*.json and no probe exist for it, and ARTIFACTS.md neither lists nor explains the absence. Every sibling Rung-4 "no longer open" item (tombstone, fork, rewind) has JSON+probe; the crash window, a headline durability defect, has neither and cannot be re-derived or re-run from the corpus.

4. Severity MED; Class OVERSTATED; ARCHITECTURE.md:744-746 (and ARTIFACTS.md:62). Claim: "A CLI session fork copied a valid `<persisted-output>` wrapper verbatim into the child transcript but created no child-owned artifact." Observed: probes/claude/persisted_output_fork.py:59,75-76,85 shows the wrapper was authored by the HTTP stub as plain assistant text, the artifact file was planted by the probe (`artifact.write_text("PARENT_OWNED_FULL_OUTPUT")`), and the run used `--tools ""` — Claude's persisted-output reduction pipeline never executed and no content-replacement record existed. The demonstrated invariant (fork copies rows verbatim, copies no artifact, parent delete dangles the path — all true in the capture) is real, but "valid wrapper" is a simulation nowhere disclosed in ARCHITECTURE, ARTIFACTS, or the report, and fork handling of genuine replacement-ledger records is untested.

5. Severity MED; Class INCONSISTENT; ARTIFACTS.md:57 vs evidence/claude/physical-gc-forced-negative.json. Claim: "Forced-gate attempt did not establish the real bytecode path; retained to prevent false claims of runtime confirmation." Observed: the JSON contains only semantic-compact success fields (first_request/compact_request/second_request true, jsonl_compact_boundary true, jsonl_summary_marker true, resume_rc 0, TUI screen_tail) and — per its generator probes/claude/physical_gc_forced_diagnostic.py:54 — measures no transcript byte sizes, no pre-boundary raw-row survival, no gate state; the data that established "did not establish the bytecode path" (/tmp/claude-physical-gc-forced-requests.json, full screen log) was not retained. The retained artifact cannot support even its indexed negative interpretation.

6. Severity MED; Class INCONSISTENT; ARCHITECTURE.md:171-172 vs :37. Claim: "**Executable-reproduced** — observed with the real 2.1.220 binary." Observed: the same document states "The installed binary is already locally patched," and clear_session_identity.py:11, rewind_successor.py:5, physical_gc_live.py:5, persisted_output_fork.py:11 hardcode that installed binary (SHA d6e888…) as the test subject; the corpus never enumerates the pre-existing patch delta or a pristine-upstream hash. Every "real binary" finding is actually a patched-binary-of-undocumented-delta finding, and an auditor cannot exclude patch interference with the probed subsystems (transcript writer, fork, /clear).

7. Severity MED; Class OVERSTATED; ARCHITECTURE.md:727-728 + ARTIFACTS.md:117-118 + probes/README.md:55-56. Claim: "A 29,273-byte transcript was killed after truncate and before suffix rewrite"; "The reports and probe descriptions state which behavior each binary forced"; "The architecture document records the structural behavior each instrumented binary forced." Observed: tombstone_removal.py:19-20,32 reveals the instrumented binary takes PROBE_REMOVE_UUID (env-triggered removal, not a natural tombstone flow) and PROBE_REMOVE_PAUSE_MS='10000' — a 10-second injected pause artificially widening the truncate→rewrite window the kill lands in; neither the pause nor the env trigger is documented anywhere, and the two meta-claims cross-reference documentation that does not exist (ARCHITECTURE records no per-binary forced behavior). Legitimate fault injection, but the corpus misrepresents its own disclosure level and the "window" is lab-forced, not measured.

8. Severity MED; Class PROBE-DEFECT; probes/claude/precompute_cross_model.py:20 + evidence/claude/precompute-cross-model.json. Claim (ARCHITECTURE.md:878-884): "precompute under gpt-5.6-luna … Terra consumes Luna summary." Observed: the stub returns the literal 'PRECOMPUTED_LUNA_SUMMARY' for EVERY summarization request regardless of model, and the capture shows compact_count:2 (a second, Terra-model compact exists), so retry_contains_luna_summary:true cannot by itself distinguish luna-era from terra-era consumption; the disambiguating request ordering lives only in the excluded raw log. I verified externally (surviving /tmp/claude-precompute-cross-model-requests.json, same-day): order is luna-compact(#3) → terra-PTL(#5) → marker-bearing retry(#6) → terra-compact(#7), so the conclusion is correct — but the retained corpus cannot re-derive it, breaking the exclusion premise "already summarized by a focused result."

9. Severity MED; Class INCONSISTENT; fixtures/cliproxy-claude-models.json vs ARTIFACTS.md:78. Claim: "Synthetic local Claude `/v1/models` response used by the retained probes." Observed: every model id is systematically transformed — reversed and prefixed "claude-fable-5-dd-" ("claude-fable-5-dd-anul-6.5-tpg" = gpt-5.6-luna, "-ukiah" = haiku, "-elbaf" = fable, "-supo" = opus, "-tennos" = sonnet) while display_name/token fields stay clean; the /tmp original is byte-identical and dated Aug 4 19:12 (pre-run), so all probes genuinely served a catalog in which the requested IDs (gpt-5.6-luna etc.) do not exist, contradicting clear_session_identity.py:15's own inline fallback catalog which uses the clean id. Nothing in the corpus mentions or explains the scheme; probe runs were inert to it only if /v1/models is never consulted, which the corpus does not establish.

10. Severity LOW; Class HYGIENE; ARCHITECTURE.md:542-543,559. Claim: "A focused CLIProxy test confirmed replay scope prefers the old header over conflicting body metadata. … Capture: evidence/claude/clear-session-identity.json." Observed: that Claude capture contains no CLIProxy data; the result lives in probes/cliproxy/executor/replay_and_count_regressions_test.go:81-93 (TestWorkflowProbeClaudeHeaderOverridesBodySession, expecting "claude:old-session:agent:main") + workflow12-local-state-checks.md:11,25 — in-corpus but uncited at the claim site, so the sole cited capture over-covers the paragraph.

11. Severity LOW; Class HYGIENE; ARCHITECTURE.md:1232 + :570-575. Claim: "the greater-than-6-MiB ordinary local fixture"; TTL table "2,933 bytes / 2,685 bytes / 2,734 bytes." Observed: physical-gc-live-negative.json before.bytes=6,003,800 = 5.73 MiB (>6 MB, not >6 MiB; substantive point survives since the stated 5-MiB eligibility threshold is still exceeded); schedule_wakeup_ttl.py:41 computes description_bytes=len(str) — character counts, not bytes (descriptions are em-dash-heavy UTF-8) — in a corpus that trades on exact byte figures.

12. Severity LOW; Class HYGIENE; probes/claude/tombstone_removal.py:15 + rewind_successor.py:52 + ARTIFACTS.md:55. Claim: rewind captures are "Executable control[s]" and tombstone probe is a retained reproduction. Observed: tombstone base_copy() hard-depends on /tmp/claude-rewind-successor-100.json and that run's intact root dir — a file the rewind probe never writes (it writes -result.json; the -{100,500,1000}.json names are undocumented manual copies), KILL_DELAY_MS is undocumented in probes/README, the 0-ms rewind result was overwritten/lost (ARTIFACTS.md:55 admits "no distinct durable result file left to copy"), and 10/25/50 ms rewind delays were never run — so the withdrawal's residual-window half (ARCHITECTURE.md:194 "only the ordinary early write window remains") rests on report prose plus interpolation from the non-rewind crash table (itself finding 3).

13. Severity LOW; Class INCONSISTENT; evidence/claude/physical-gc-live-negative.json. Claim (ARTIFACTS.md:56): "Normal local environment-variable setup did not physically prune raw rows." Observed: top-level compact_seen:false coexists with requests[2].compact:true in the same file — the probe (physical_gc_live.py:32) sampled the flag after a 300 s wait, before the lazily-triggered compaction arrived with the next turn; the negative itself is genuine (before/after raw:true) but the artifact is self-contradictory without the probe source.

CLEAN

- clear-session-identity.json: normal run rotates BOTH header and body (11111111-1111-4111-8111-111111111111 → ab73acfa-… in X-Claude-Code-Session-Id AND metadata.user_id.session_id); pinned run keeps header 11111111-… while body rotates to 96444f77-… — matches ARCHITECTURE.md:539-542 exactly.
- context-hint-durability.json: 402,116 (requests[2], hint enabled, raw_A 30) / 253,221 (requests[3], wrappers:5) / 402,241 (requests[4] resume, raw_A 30, hint.enabled true = "offered again: yes") — all three byte figures at ARCHITECTURE.md:858-862 exact. The "raw results returned next turn in same process" sentence (:869-871) is not in this capture but is supported by context-hint-precompute-stale.json clear_requests[2] (402,465, raw 30, after the 253,465 wrapper retry) + outstanding5:58.
- precompute-cross-model.json: models [luna×3, terra×4], ptl_sent/retry_seen true, trigger_models [terra, terra], retry_contains_luna_summary true — supports :878-884 subject to finding 8.
- context-hint-precompute-stale.json: precompute_request/hint_rejected/hint_retry/ptl_rejected/ptl_retry all true; wrapper retry (253,465, wrappers 5) → next-turn raw restore (402,465, raw 30) → PTL retry consumes pre-hint marker (398,408, marker true) — matches :887-895.
- tombstone-removal.json: 53,069,996 → 53,071,918, target_remains true, rc 0 — exact match to :723-726 (64-KiB-fallback sentence is source-derived explanation). Bonus: the file also honestly retains the non-truncating control (ARTIFACTS.md:60's description matches).
- tombstone-interruption.json: 29,273 → 9,020, truncated_before_kill true, target_remains false AND suffix_remains false — exact match to :727-729.
- persisted-output-fork-ownership.json: wrapper_copied_verbatim true / child_owned_artifact_copy false / artifact_exists_after_parent_delete false / child_transcript_exists_after_parent_delete true — matches :744-758 (subject to finding 4's simulation caveat).
- rewind-successor-{100,500,1000}ms.json: all three show resume_has_NEW_BRANCH true, resume_has_THIRD false, jsonl_has_NEW_BRANCH true with distinct session/target UUIDs — the withdrawal's positive half ("once the new user record commits, the tested branch resumes correctly") is capture-backed at those delays.
- schedule-wakeup-ttl-variants.json: 2,933/2,685/2,734 with has_5m/has_1h/has_generic flags and per-variant SHA-256s matching outstanding5's table — matches ARCHITECTURE.md:570-575 (unit caveat in finding 11).
- schedule-wakeup-token-counts.json: 1,849/1,777/1,804 — exact.
- manual-compact-requests.json wire-proves all four manual-compact properties (:777-784): req4 = summary request, req5 = same-process use (marker), req6 = fresh-process resume ("after restart" + marker, 7 messages). semantic-auto-compact-control.json shows the automatic side: compact_like_requests 1, jsonl boundary+summary true, wire marker on requests 5-6, resume_rc 0 (request 6 matches the resume-request pattern of the manual harness).
- Physical-GC negatives: ARCHITECTURE never cites either as positive confirmation (:798-800 "negative diagnostics, not confirmations"; :1229-1233 consistent) — mapping 12's first half passes.
- (c) 128k/32k: supported twice — workflow12-local-state-checks.md:87 AND incidentally by clear-session-identity.json itself (bodies carry max_tokens:128000; output_tail modelUsage carries maxOutputTokens:32000). Not an unsupported claim.
- Probe hygiene: all Claude probes bind 127.0.0.1 only and write results to /tmp; tombstone_removal.py verifies via independent read-back (stat + full-content grep for target/suffix), NOT return codes (rc recorded but not the success criterion); rewind_successor.py's hardcoded new_request_seen:True is guarded by a raise at :43.
- Report arithmetic: "6/6 passed" vs seven listed behaviors resolves cleanly — six test functions, the first covers two bullets; TestWorkflowProbeClaudeHeaderOverridesBodySession exists and encodes header-over-body precedence.
- executor-suite.log contains exactly the three named device-profile failures and no others; ARTIFACTS description accurate.
- SHA256SUMS: all 43 hashes verify; coverage is complete (43 hashed = 43 files excl. the manifest). Byte-copy claim spot-verified: clear, tombstone-removal, rewind-100, ttl-variants corpus files are byte-identical to surviving /tmp originals.
- The retained Go replay test's undefined-looking helper validCodexReasoningEncryptedContentForTestSeed is defined upstream (/tmp/cliproxyapi-source/internal/runtime/executor/codex_executor_reasoning_replay_cache_test.go:24), so the README copy-in rerun instruction is executable — checked and dismissed.

GAPS

- Nothing in the corpus lets me determine what the pre-existing local patches in the installed 2.1.220 binary change (finding 6); ditto the exact edits of the six instrumented binaries (excluded by design; descriptions partial — findings 5, 7).
- I did not execute any probe or test (per instructions); Go compile-ability judged statically plus the upstream-helper grep.
- semantic-auto-compact-control.json has no per-request process tags, so "request 6 = resume request" is pattern-matched from the sibling harness, not labeled data.
- Sub-100 ms rewind behavior (0/10/25/50 ms) has no auditable capture anywhere (finding 12); the 0 ms result exists only as report prose.
- The user's /tmp/claude-2.1.220.js bundle could in principle re-derive findings 1-2's values, but that is corpus-external; my UNSUPPORTED calls are about corpus self-sufficiency and I did not attempt re-derivation.
- CLIProxy-side Go test internals beyond the replay file were only skimmed for ARTIFACTS-description match (they match: credential log shows the two PASSing stale-handshake tests; partial-write log shows reset→reconnect; late-frame JSON shows resp_A_LATE delivered as B's terminal) — deep verification belongs to the cliproxy beat.
